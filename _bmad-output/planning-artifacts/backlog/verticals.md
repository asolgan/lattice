# Backlog — App Verticals (Stream 1)

The **App Verticals** lane: package + FE work for **LoftSpace** & **Clinic**. Advanced by the **Vertical
Steward** (`steward-verticals`); hydrated by the **Vertical PO** (`vertical-po-discovery`). Scales, status
vocabulary, and cross-lane rules: see [../backlog.md](../backlog.md). Design:
`implementation-artifacts/agentic-ops-swimlanes-design.md`.

**No-paper-over rule (binding):** a missing Lattice capability is filed to [lattice.md](lattice.md) as platform
demand and the vertical item is marked `🚧 blocked-on:` it — **never** hacked around in the package/FE and
called done. A vertical builds *on* real Lattice capabilities.

Status: 📋 ready · 🏗️ in-flight (worktree) · ✅ done (commit) · 📐 design-proposed · 🚧 blocked.

---

## Vertical demand backlog (PO discovery)

Filed by the Vertical PO discovery loop (demand side). Each item is tagged with the **vertical** and
the **owner** (FE = Sally + FE Engineer · pkg = Package Designer · platform = component owner). Scored
Imp ★ / Size. The Steward + FE Engineer pick these up; the PO only files.

| Item | What it is (PO view) | Vertical | Owner | Imp | Size |
|---|---|---|---|---|---|
| Property / Unit / Listing domain + richer application | ✅ **Design build-ready** (`implementation-artifacts/loftspace-property-domain-design.md` — decisions resolved by PO+Winston, adversarially reviewed + hardened, no contract change / no Andrew gate; new `loftspace-domain` pkg owning `vtx.unit`, `unit` required at create, convergence-lens walks to it for display). ✅ Increment 1 (the `loftspace-domain` package — `.listing`/`.address` aspects + `SetListing`/`SetUnitAddress`) **DONE + live-verified**; ✅ Increment 2 (lease-signing integration: required `unit` at create + `appliesToUnit` link + `.terms` aspect + convergence-lens walk projecting `unitKey`/`unitAddress`/`unitRent`) **DONE (047a67e), cross-package e2e green**; 🏗️ Increment 3 (applicant FE) next — now unblocked. The biggest *product* gap: `vtx.leaseapp.<id>` is a bare shell (root `{}` + one `applicationFor` link). The vertical models the *workflow* but not the *thing being leased* — there is no property/unit/listing, no rent, lease term, move-in date, applicant income/employment, co-applicants or guarantor. "What am I even applying to lease?" is unanswerable today. Needs a `loftspace-domain` (or similar) package: a unit/listing vertex type + application-detail aspects, with the convergence lens able to walk to the unit. Foundation for a real applicant app. | LoftSpace | pkg + FE | ★★★ | L |
| Decline / manual-review application outcome | **Re-scoped 2026-06-25 (Steward) — part (1)+(3) the disposition SURFACE is now ✅ Done (3cff020, Progress board above); the residual is part (2), the live decline TRIGGER.** The auto-approve correctness defect was already fixed (828f24d: `RecordLeaseServiceOutcome` validates `status ∈ {completed,failed}`, the lens counts only `completed`, a failed check stays violating — pinned by `lens_cypher_test.go`). ✅ **(1) terminal disposition + (3) lens/FE surface DONE:** the lens projects `declined_*` and the My Applications FE renders a red "Application declined" banner / Declined step (no longer "blocked-forever-invisible"). ✅ **(2) drive a decline LIVE — Done (global demo toggle)**, see the Progress board row "drive a decline LIVE (demo affordance)": `BRIDGE_FAKE_DECLINE` makes the fakes decline every check, so an operator drives the declined experience end-to-end (live-verified: app row `declined=true`, FE red banner). **Residual — a *per-applicant selective* decline (★, the harder seam):** so different applicants get different verdicts on one running bridge. Needs an applicant-controlled signal (e.g. the applicant's `.name`) threaded from the `CreateLeaseServiceInstance` instanceOp → externalTask `params` → `Request.Params` → the fake — which requires Loom to hydrate the subject's `.name` aspect into the dispatch (today `reads=[subjectKey]` only, `engine.go:1009`, contract-guarded; root is `{}` per D5). A Loom dispatch-reads / §10.5-adjacent change — its own fire. | LoftSpace | platform (Loom) | ★ | S–M |
| Human-readable task content on userTasks / assignTasks | **Re-scoped 2026-06-25 (Winston) — NOT a platform/package gap; the content is reachable today.** Task *relationships are links* by design (Contract #10 §10.1): the `my-tasks` lens already projects `forOperation: vtx.meta.<id>`, and that op meta-vertex carries `.canonicalName` + the DDL self-description (inputSchema / fieldDescription / examples) Loupe **already reads to render op-submit forms**. So a task-inbox renders {op canonicalName = title, op description = instructions, op inputSchema = the form} by resolving the `forOperation` meta — the same Core-KV meta-read pattern Loupe's `resolveLens` already ships. Decision: **project the op's human label from the `forOperation` meta, do NOT stamp a `.prompt` aspect** (single source of truth; no dual-write; preserves the task root's deliberate `{status,expiresAt}`-only / NO-aspects invariant; no §10.8 dispatch-`reads` / contract touch). ✅ **Platform fix DONE (corrected 2026-06-25)** — the `my-tasks` lens projects `operationName ← op.data.operationType` (the field every op DDL carries) and `operationDescription ← op.description.data.value` (best-effort), so the per-identity read-model row is self-describing for **every** consumer. *(The earlier `op.canonicalName.data.value` hop projected null in the real flow — a dispatched userTask's `forOperation` points at the op DDL meta whose name is the root `operationType`, NOT a `.canonicalName` aspect; the old e2e masked it by manufacturing the aspect. Fixed + guarded by a realistic e2e.)* ✅ **Loupe operator task-inbox data layer done** (`GET /api/tasks`: all tasks across identities, link-sources assignee/op/target, resolves the op label from root `operationType` with a `.canonicalName` fallback; unit-tested). Residual: the optional op-`description` authoring nicety (ensure each userTask op DDL carries a human `description` aspect, not just the machine `operationType`). | LoftSpace (all) | FE | ★★ | S |
| LoftSpace applicant app — scoped FE | ✅ **Done — Increments A–E all SHIPPED (in-browser verified). The greenfield applicant FE is complete: Browse&Apply, My Applications, Tasks, Documents, + `make up-loftspace` local-stack wiring.** ✅ **UX design build-ready** + ✅ **Increment A** (scaffold + Browse&Apply) + ✅ **Increment B** (My Applications status tracker: `/api/applications` lens read + journey stepper, 32ffcca) + ✅ **Increment C** (task inbox over the `my-tasks` lens + `RecordIdentityPII`/`SignLease` completion verified through the real Processor, ced082d). Remaining: D (documents, objects-base) · E (up-full wiring). Prerequisites met: property-domain (Increments 1+2) shipped + task-content self-describing. **Verified live: the vertical is headless** — every step (apply, run checks, complete PII, sign) had to be driven via `lattice op submit` as the system actor; an applicant has no way in. Scoping the greenfield FE the *Now* section flags generically: (1) application-intake form (driven by DDL self-description), (2) "my application status" tracker (submitted → checks → sign → decision), (3) task inbox to complete `RecordIdentityPII` + `SignLease`, (4) document upload (ID / lease PDF, via objects-base). Default to Loupe's vanilla HTML/CSS/JS stack. **Next pick: FE Increment A (scaffold + intake).** | LoftSpace | FE | ★★★ | L |
| Close assignTask tasks when their gap is satisfied | ✅ **Resolved (Steward + Andrew, 2026-06-25) — small app-level fix; the proposed Weaver core-orchestration change RETRACTED as over-built.** Andrew flagged that the Processor *already* auto-completes the task on the §10.7 **task path** (an op authorized via the task's ephemeral grant → commit injects `status→complete` + `taskCompleted`, atomic, CAS-on-open; `internal/processor/autocomplete.go`). The lingering task is the **no-hint case**: the `loftspace-app` submits every op as the **primordial admin actor** (standing permission), never the task grant, so auto-complete never fires. In the proper per-identity flow (deferred read-path-auth) it closes for free. **Fix:** the Tasks-tab completion now submits an explicit **`CompleteTask(taskKey)`** (the §10.7 retained out-of-band path) right after the bound op commits — `cmd/loftspace-app/web/app.js`, best-effort (benign already-closed rejection logged, not surfaced). No Weaver change, no §10.8 contract question, no frozen-contract touch. The Weaver-reconciliation design is retained as RETRACTED in the doc for the record. Ref: `implementation-artifacts/task-closure-on-gap-satisfaction-design.md`. | LoftSpace (all) | FE | ★★ | XS |
| `clinic-domain` package — patient / provider / appointment / visit model | ✅ **Increment 1 DONE (Steward, 2026-06-25)** — the self-contained `packages/clinic-domain` ships `vtx.patient`/`vtx.provider`/`vtx.appointment` + their aspects/links + 7 ops + 2 P5 projection lenses (`clinicAppointments` joins patient↔appointment↔provider, `clinicProviders` roster); 3-layer-reviewed, all gates green, `make install-clinic`. See the Progress board row + `implementation-artifacts/clinic-domain-design.md`. The **convergence-lens** the PO asked for ships as a **projection** (Increment 1 has no orchestrated workflow yet — a Weaver convergence lens follows the scheduling/`@every` work). **Remaining (later increments):** scheduling/availability aspects + double-book + `@every` (the platform gaps below) and the clinic FE (UX→FE). *Original gap (resolved by Inc 1): the forcing-function vertical had zero domain — nothing bookable.* | Clinic | pkg + FE | ★★★ | L |
| Recurring `@every` schedules — the clinic forcing function | **Static, verified by grep: `@every` has NO consumer.** The `core-schedules` stream is bootstrapped with `AllowMsgSchedules` (primordial.go:198 advertises `@at/@every`), but every component — bridge poll/timeout, the Weaver temporal lane — uses **`@at` one-shot only** (contract §10.4: "Phase 2 uses @at one-shot"). Clinic is the demand that pulls `@every` into existence: appointment reminders ("remind 24h before"), recurring provider availability ("Dr.X Mon/Wed 9–5"), recurring follow-ups. This is the deferred platform work the clinic vertical exists to force (agentic-ops-design §5/§11). Build the recurring-schedule emit + re-arm path on the temporal lane and a vertical that uses it. **Scope sharpened (Steward, 2026-06-25, grounded for the next design fire):** (1) **`@every` is frozen-contract-adjacent** — §10.4 (FROZEN) already names the `@every` header shape ("or @every for recurring — Phase 2 uses @at one-shot"), so the *header* needs no change, but the **re-arm / dedup / drift semantics** of a recurring fire (does the scheduler republish the next occurrence? dedup key per occurrence?) are unspecified → that specific part is a **§10.4 amendment = Andrew gate** (design it, flag that part 📐, build the rest). (2) **Appointment *reminders* ("remind 24h before") are `@at` ONE-SHOT, NOT `@every`** — schedule a single timer at `startsAt − 24h`; that slice is **build-ready on the existing temporal lane** (no contract change), needing only a Weaver convergence lens ("appointment scheduled, reminder not yet sent") + a dispatch path. **Only recurring availability / recurring follow-ups genuinely need `@every`.** ✅ **Part (2) — the `@at` one-shot appointment-reminder slice — SHIPPED (Steward, 2026-06-26): the `clinic-reminders` package** (see the Progress board row). **Remaining = part (1) only:** the recurring `@every` re-arm/dedup/drift semantics (the §10.4-amendment Andrew gate) + a recurring use (provider availability / recurring follow-ups). The reminder pattern (freshUntil → @at → MarkExpired → reproject → directOp) is now a proven in-vertical template for the recurring design to build on. | Clinic | platform (orchestration / Weaver) | ★★★ | M |
| Appointment scheduling — conflict + temporal availability | ✅ **DONE — Increment 1 (CreateAppointment double-book) + 2a (reschedule-into-conflict) + 2b (provider availability windows) ALL shipped (Steward, 2026-06-26)** — see the Progress board rows. `CreateAppointment` + `RescheduleAppointment` reject an overlapping slot (`SlotConflict`, via the provider `.bookings` OCC index + `kv.Read` liveness) AND an out-of-business-hours booking (`OutsideHours`, via the opt-in `.hours` windows — enforced with the new pure `time.weekday`/`time.seconds_of_day` Starlark builtins). No platform scan seam, no frozen contract (Capability-KV §06's "operation's own Starlark logic"). The §06 conflict+availability gap is **closed**. Remaining clinic-scheduling work is the separate recurring-`@every` item + available-**slot** picking (a later FE increment over this enforcement). **Static, contract-grounded.** Capability-KV §06 (L354–359) **explicitly defers** temporal availability (recurring schedules, availability windows, double-book rejection) out of Phase-1 scope to "a Phase 2 mechanism / the operation's own Starlark logic." A clinic `BookAppointment` op must reject a slot that's already taken or outside provider hours — there is no slot-uniqueness or availability-window enforcement primitive today. Clinic forces this gap concrete: provider-hours + slot-uniqueness enforcement at op time (and surfacing "why was this rejected" to the booker). **Scope sharpened (Steward, 2026-06-25, grounded):** op-time conflict detection must read **"the provider's other appointments"** — but package Starlark is **known-key-reads only** (no prefix scans; `TestPackage_NoScans` enforces it across every domain pkg), and §06 (FROZEN) explicitly defers this to "a Phase 2 mechanism or the operation's own Starlark logic." So the missing primitive is a **platform read seam in the op path** (e.g. an adjacency/lens query the op can call to enumerate a provider's appointments in a window) — that is the "platform" half of `pkg + platform`, and the design's load-bearing decision. The clinic-app FE already records a *requested* time and says so ("availability isn't enforced yet"), so the FE is ready to surface a rejection once the op can produce one. | Clinic | pkg + platform | ★★★ | M |
| Clinic FE — patient booking + provider schedule | 🏗️ **Increment A SHIPPED (Steward, 2026-06-25)** — `cmd/clinic-app` on :7799 (UX→FE→in-browser-verified): (1) patient self-booking (provider picker + datetime → `CreateAppointment`; + inline add-provider / new-patient affordances), (2) "my appointments" tracker (patient-scoped cards + Cancel), (3) provider schedule (provider-scoped read-only). All P5-clean lens reads (added the `clinicPatients` roster lens). `make up-clinic`. See the Progress board row + `implementation-artifacts/clinic-app-ux.md`. ✅ **Reschedule (move an appointment) SHIPPED (Steward, 2026-06-26)** — new `RescheduleAppointment` op + a Reschedule modal on active cards; re-arms the `@at` reminder for the new time (see the Progress board row). ✅ **Provider day/week calendar grid SHIPPED (Steward, 2026-06-26, daecf5a)** — the Schedule tab is now a positioned Week/Day calendar (lanes for double-books, period nav, click-to-detail; in-browser verified). See the Progress board row. ✅ **Available-**slot** picking SHIPPED (Steward, 2026-06-27, ecad67c)** — the Book form's "Find available times" picker projects the provider's `.hours` into the `clinicProviders` lens and suggests open slots (hours − timeoff − existing appointments − past) as clickable buttons; see the Done log. ✅ **Slot-picker blocked-date reasons SHIPPED (Steward, 2026-06-27, fff351d)** — the picker's empty state now NAMES why a date has no slots (provider doesn't work that weekday → lists working days · on time-off that whole day → with reason · else the generic booked/duration line); see the Done log. **Remaining (later increments):** clinic-admin slot & availability management + actually *disabling* (graying-out) past/blocked dates in the native date input itself — the native `<input type=date>` can't exclude arbitrary dates, so that needs a custom calendar; the now-shipped reason text + the `min` past-guard mitigate. Default stays Loupe's vanilla HTML/CSS/JS stack. | Clinic | FE | ★★★ | L |
| LoftSpace landlord / property-manager surface | ✅ **DONE — Inc 1 (data layer) + Inc 2 (decision op + convergence-lens landlord-gating) + Inc 3 (the landlord FE) ALL shipped.** ✅ **Inc 3 (Steward, 2026-06-27, db4073a):** the landlord FE — a top-bar **Applicant ⇄ Landlord** mode toggle (persisted) swaps the applicant tab strip for a **my-units view** over the already-shipped `GET /api/unit-applications` (P5: three lens read models, never Core KV). Each unit card = address / rent / status / application count + the live applications, each with a **disposition badge** (qualified / in-review / approved / leased / declined) + a signed marker; a **qualified** application (the landlord-decision state) gets **Approve / Decline** buttons wiring `DecideLeaseApplication`, and once a unit leases the other applicants read "unit leased to another applicant" with no action buttons. A **Post-a-listing modal** mints + lists a unit via the existing `CreateLocation` → `SetUnitAddress` → `SetListing` op chain. Pure FE (no Go handler change); gates green (build / vet / golangci-lint / P5 lint-conventions STRICT / `go test ./cmd/loftspace-app/...` / `node --check`); the JSON shape cross-checked against the `unit_applications.go` DTO. **Full in-browser click-through verify deferred to an attended pass (F-004 — the shared :7788 app is a stale binary this run did not start, so could not be restarted to serve the new assets).** Design: `implementation-artifacts/loftspace-landlord-surface-design.md` Inc 3. ✅ **Inc 2 (Steward, 2026-06-26, 6c30a10):** new `DecideLeaseApplication{leaseAppKey, decision}` op (operator-granted) writes a `.decision` aspect (unconditioned upsert → decline↔approve reversible); the `leaseApplicationComplete` lens gains `landlordDecision`/`landlordApproved`/`landlordDeclined`/`missing_decision`. **`missing_listingLeased` now requires BOTH applicant-readiness AND `landlordDecision='approved'`** (Winston ruling — the design's "replace the applicantApproved term" was imprecise; the 3-layer review caught that gating on landlord-approval alone would lease a unit to an unqualified/stale applicant). `missing_decision` keeps a qualified-but-undecided app violating with NO dispatch (closes the auto-lease race); landlord-decline is terminal-not-violating; `declined` folds in a landlord decline. loftspace-app surfaces the columns (applicant banner = "Qualified — awaiting landlord review" / "Approved — finalizing lease" / "complete"; the by-unit view carries the per-applicant disposition). No frozen contract (Cap-KV §06 + §10.2 one-row-per-anchor preserved); 3-layer-reviewed (2 blocking bugs caught + fixed: dead FE columns at the DTO layer, the readiness-gating regression); all gates green incl. the 109s `test-lease-convergence` e2e; lease-signing 0.3.0→0.4.0. In-browser verify deferred to the Inc-3 FE fire (F-004). Data layer (`GET /api/unit-applications`) ✅ Done (e6d4499). Filed 2026-06-26 (PO discovery, live). The vertical is **one-sided** — `loftspace-app` is entirely applicant-facing. There is **no landlord/property-manager surface**: no way to (1) **post or manage a listing** — I had to mint the unit + listing via the raw op API (`CreateLocation` → `SetListing` → `SetUnitAddress`); (2) ✅ **see who applied to a unit** — **DONE**: the new P5-clean `GET /api/unit-applications` groups the existing `leaseApplicationComplete` rows by `unitKey` (+ applicant names from `applicantRoster`, + zero-app units from `availableListings`), so the by-unit applicant view exists at the data layer with **no new lens needed** (the convergence lens already carries unitKey/applicant/approval/declined — the "by-unit projection lens" the original filing guessed at was unnecessary); (3) **make a leasing decision** — convergence == auto-approval, the only "decline" path is the `BRIDGE_FAKE_DECLINE` env toggle, not a human review. **Remaining:** a landlord/operator **FE** (list-my-units over `/api/unit-applications`, a post-a-listing form over the existing `CreateLocation`/`SetListing`/`SetUnitAddress` ops, applicants-per-unit cards, approve/decline) — a **UX→FE** increment — **and** an explicit landlord-**decision op** (the harder half: an approve/decline that overrides auto-converge). The single biggest *product* gap now that the applicant FE is complete. | LoftSpace | FE + pkg/platform | ★★★ | L |
| LoftSpace — in-app applicant onboarding (no way to add an applicant) | ✅ **Done (Steward, 2026-06-26)** — a **"＋ New applicant"** modal (name/email/phone) now creates an applicant in-app via `CreateUnclaimedIdentity`, minting + hashing the claim secret client-side (`crypto.subtle`); the new applicant becomes active and projects into the `applicantRoster` lens. In-browser verified end-to-end (created "Dana Tester" → switcher + Apply enabled). See the Progress board row. **Original filing:** A fresh LoftSpace stack has **zero applicants and no in-app way to create one** — the top-right control is only a **switcher** over already-existing named identities (`applicantRoster` lens). To exercise the flow I had to mint Priya via a raw `CreateUnclaimedIdentity` requiring `email`/`phone` **plus a 64-char lowercase-hex sha256 `claimKeyHash`** — an identity-domain ceremony no applicant could perform from the app. Contrast: **`clinic-app` already ships a "New patient" modal** (`CreatePatient`) so a fresh stack is self-contained; loftspace-app has no equivalent. Needs an in-app "Start an application / new applicant" affordance (a thin modal mirroring clinic's New-patient) that handles the identity ceremony app-side (name/email + a generated claimKeyHash). Makes the app demoable on a clean stack without the CLI. | LoftSpace | FE | ★★ | S–M |
| LoftSpace — unit/listing lifecycle: status transition on lease + concurrent-application guard | ✅ **Done — Part (a) listing-status-on-approval (via landlord Inc 2 `6c30a10`) + Part (b) the concurrent/duplicate-application guard (Steward, 2026-06-26)** — `CreateLeaseApplication` now rejects a same-applicant repeat for the same unit (`DuplicateApplication`) via a per-unit `unitLeaseApplications` `kv.Read`+OCC index (clinic-double-book precedent, no frozen contract, fail-closed; cross-applicant still allowed); also closes the folded Weaver-red-on-bare-shell robustness symptom. See the Progress board row. **Part (a) listing-status-on-approval ✅ Done** — shipped folded into the landlord surface Inc 2 (`6c30a10`): the convergence lens's `missing_listingLeased` gap drives `directOp(SetListingStatus status=leased)` over the leased unit (loftspace-domain's `SetListingStatus` op + the `unitStatus` lens column already exist; verified in `targets.go`/`lenses.go`). So a landlord-approved + applicant-ready unit flips its `.listing.status` to `leased` and drops out of Browse&Apply. **Both parts now done → this row is ✅.** **Filed 2026-06-26 (PO discovery, live — two related gaps on one converged application).** (a) **Listing status never transitions on approval:** after Priya's application **fully converged** (`violating:false`, all gaps closed = approved), the unit's listing stayed `status:"available"` and kept showing in Browse&Apply — nothing flips a leased unit to `pending`/`leased`. (b) **No concurrent/duplicate-application guard:** the *same* applicant applied to the *same* unit a **second time and it committed** (two live `vtx.leaseapp` for one applicant+unit); nothing limits concurrent applications per unit or reflects a unit being spoken-for. Clinic has the precedent for the platform half — op-time conflict rejection via `kv.Read` on a provider's `.bookings` index (no frozen-contract touch, Capability-KV §06 "operation's own Starlark logic"); LoftSpace has **no analog** for a unit's applications. Needs: a listing-status transition tied to approval (convergence → `directOp(SetListing status=leased)`, the `objectLiveness→TombstoneObject` / `appointmentReminders` precedent — OR a landlord action from the surface above) **and** an op-time guard on `CreateLeaseApplication` (reject/limit live applications per unit via a `kv.Read` on a unit→applications adjacency aspect, mirroring clinic's double-book). | LoftSpace | pkg + platform | ★★ | M |
| Clinic — appointment lifecycle / day-of-visit actions (confirm · check-in · complete · no-show) | ✅ **DONE — FE transitions (2026-06-26) + the `checkedIn` status & cancel/no-show audit note (2026-06-26) both shipped.** confirm/complete/no-show reachable from the My Appointments cards + the Schedule detail popover (wiring the existing `SetAppointmentStatus`); in-browser verified scheduled→confirmed→completed through the real Processor. ✅ **The ★ pkg follow-on is now also DONE** (see the Progress board row "Clinic app — `checkedIn` status + audit note"): the status enum gained `checkedIn` (active, non-terminal — scheduled→confirmed→checkedIn→completed), `SetAppointmentStatus` gained an optional `note` on `.status` (cancel/no-show reason, surfaced via a new `statusNote` lens column + rendered 📝 on cards/popover), with a Processor-driven test. No remaining day-of-visit gap. **Original filing:** The booking side is well-built, but the **day-of-visit operational core is absent from the FE.** `clinic-app` exposes only **Cancel** and **Reschedule** on an appointment — there is **no way to mark it `confirmed`, `completed`, or `noShow`** (no check-in either). Yet every piece is already in place: `SetAppointmentStatus` accepts all five statuses `{scheduled, confirmed, completed, cancelled, noShow}` (verified live: confirmed an appointment via `/api/op` → projected `confirmed`), the `clinicAppointments` lens already projects the `status` column, and the FE already *renders* per-status badges + calendar colors (`statusClass` handles `confirmed`/`completed`/`noshow`). So the FE renders states it gives no way to reach — a clinic where you can't confirm a patient, check them in, complete a visit, or record a no-show. **Pure FE**: add status-transition controls to the My Appointments cards and the Schedule-grid detail popover (the natural home is a front-desk/provider "today" operational view), wiring the existing `SetAppointmentStatus` op. **Small package follow-on (pkg, ★):** `SetAppointmentStatus` writes only `{value}` — a cancellation / no-show captures **no reason or note** (a real clinic needs cancel/no-show reasons for audit + billing); add an optional `reason` field to the status aspect. Distinct from the existing "Clinic FE — patient booking + provider schedule" row (whose remaining items are available-*slot* picking + admin availability mgmt — not lifecycle transitions). | Clinic | FE + small pkg | ★★★ | S–M |
| LoftSpace — no way to withdraw / cancel a lease application | ✅ **Done (Steward, 2026-06-26)** — new lease-signing **`WithdrawLeaseApplication{leaseAppKey, unit}`** op (granted `operator`): soft-deletes the leaseapp (the convergence lens filters isDeleted → the row drops from My Applications) AND prunes its entry from the unit's `unitLeaseApplications` index (OCC-guarded, mirroring the duplicate guard) so the applicant can re-apply to the same unit. Verifies the unit is the application's actual unit via the `appliesToUnit` link (kv.Read, the clinic `withProvider` precedent → `UnitMismatch` reject, no tombstone). Non-cascading tombstone; the Processor permits the index-aspect write regardless of unit liveness (no dead-parent gate in step6/step8 — verified). **FE:** a red "Withdraw application" button on each in-review My Applications card (hidden once `applicantApproved`), with a confirm → `/api/op` → reload. Processor-driven `TestWithdrawLeaseApplication` (wrong-unit→UnitMismatch+no-tombstone · correct→tombstone+prune · re-apply→accepted · double-withdraw→UnknownLeaseApplication). lease-signing 0.2.0→0.3.0 (manifest+Package synced; install round-trips through the `test-lease-convergence` e2e). No frozen contract (Capability-KV §06). Thorough lead review (destructive paths walked adversarially; mirrors the 3-layer-reviewed duplicate-guard + clinic mechanisms); all gates green. In-browser verify deferred to a fresh stack (F-004). **Original filing:** An applicant can **apply but cannot withdraw/cancel** an application. There is **no `WithdrawLeaseApplication` op** in lease-signing (grep: only `CreateLeaseApplication`/`SignLease`) and **no FE affordance** (the only DELETE in `app.js` is document-detach). This is now sharper because of the just-shipped **duplicate-application guard**: `CreateLeaseApplication` rejects a same-applicant repeat for a live unit, so an applicant who applied to the wrong unit — or wants to back out — is **stuck**: can't withdraw, and (if they want to re-apply to the same unit) can't re-apply either. The guard's own design already anticipates this — its `TombstonedPriorApplication_AllowsReapply` test tombstones the prior application via the **generic core `DeleteVertex`** to prove the unit-index prunes it and unblocks re-apply — but there is **no user-facing path** to that tombstone. Needs: a package **`WithdrawLeaseApplication(leaseAppKey)`** op (tombstone the `vtx.leaseapp` + prune the `unitLeaseApplications` index entry, mirroring the duplicate guard's OCC index rewrite — Capability-KV §06, no frozen contract) and a "Withdraw" button on the My Applications card. The missing complement to the duplicate guard; basic lifecycle. | LoftSpace | pkg + FE | ★★ | S–M |
| LoftSpace — the signed lease is content-free (no lease document to review or produce) | ✅ **DONE — Increment A (terms-review panel, 777d180) + Increment B (produce + attach the executed lease, 426f4eb) both shipped.** ✅ **Increment B (Steward, 2026-06-27, `426f4eb`, CI green):** the applicant now *receives* an executed lease. The `leaseApplicationComplete` lens projects `signedAt` (the `.signature` execution date) as a read-only scalar (mirrors Inc A — feeds no gap; `missing_signature` already derives from `signedAt=null`). New `GET/POST /api/lease-document`: GET streams a deterministic plain-text executed-lease summary (parties / premises / terms / execution date) built on-demand from the projected terms + the applicant's roster name (P5 — no Core KV, no projection lag); POST durably attaches the **same** bytes to the leaseapp under a `signedLease` slot via objects-base (deterministic store name → re-produce overwrites, never orphans; content-derived requestId → idempotent attach; reclaims bytes on a rejected attach, the upload-handler pattern). FE: a "📄 Signed lease" download link on every signed application card + a best-effort durable attach fired when SignLease commits. lease-signing 0.5.0→0.6.0. No frozen contract (Cap-KV §06; §10.2 one-row-per-anchor preserved); thorough lead review (deterministic-render/idempotent-attach/orphan-reclaim seams walked; well-scoped projection-add + new endpoint + FE, no security plane). All headless gates green (build/vet/golangci-lint/lint-conventions STRICT/go test loftspace-app+lease-signing incl. the signedAt cypher assertion/node --check) + the 108s `test-lease-convergence` e2e. In-browser verify deferred (F-004 — lens change won't hot-reload + the shared :7788 binary is stale, not started by this run). **Increment A — the lease-terms review panel — ✅ Done (Steward, 2026-06-26, `777d180`).** The applicant can now *read* the terms before signing. The `leaseApplicationComplete` convergence lens now projects the full lease economics as pure read-only scalar columns (the applicant's own requested `.terms` — moveInDate/leaseTermMonths/requestedRent — plus the rest of the unit's `.listing`: currency/bedrooms/bathrooms/leaseTermMonths/availableFrom, and `.address`: city/region); none feeds `violating`/any gap (an application with no `.terms` degrades to null terms columns). loftspace-app's My Applications card renders a **"Lease terms"** review panel (headed *"review before signing"* while the sign step is open) — property / size / rent (with the applicant's offered rent when it differs) / term / move-in, rendering only present fields. No frozen contract (Cap-KV §06; §10.2 one-row-per-anchor preserved); lease-signing 0.4.0→0.5.0. Thorough lead review (well-scoped projection-additions + FE panel, no contract/security plane); all gates green incl. the 107s `test-lease-convergence` e2e + new cypher assertions for every new column. In-browser verify deferred (F-004 — lens change won't hot-reload + the shared :7788 binary is stale, not started by this run). **Remaining = Increment B:** produce + attach a signed-lease *artifact* on SignLease (app-side `AttachObject` of a generated lease summary, or a pkg directOp) so both parties have the executed document. **Original filing (2026-06-26, PO discovery, live):** The applicant **"signs a lease" they never see, and no signed artifact is produced.** `SignLease` writes only a `.signature` aspect `{signedAt}` (verified: `packages/lease-signing/ddls.go`) — it renders no lease agreement to review **before** signing and attaches no signed document **after**. Yet the Documents tab offers a **"Signed lease (PDF)" upload slot** that is a pure manual orphan, disconnected from the workflow — the system never generates the thing that slot is for. For a leasing product this is a core gap: there is no lease the applicant can read (terms, rent, dates), and after approval the My Applications card just says "Application complete — all steps done" with no downloadable lease. The plumbing already exists: the unit `.listing` (rent/term) + the leaseapp `.terms` (moveInDate/leaseTermMonths/requestedRent) project today, and the app already does `AttachObject` against objects-base in the Documents tab. Needs: (FE) a **lease-terms review panel** on the Sign-lease task (renders the projected terms the applicant is agreeing to), and (FE/pkg) **produce + attach a signed-lease artifact** on SignLease (app-side `AttachObject` of a generated lease summary/PDF, or a pkg directOp) so both parties have the executed document. Distinct from the storage-substrate "Large-file/binary handling" row (that shipped objects-base; this is wiring a *generated lease doc* into the sign workflow). | LoftSpace | FE + pkg | ★★ | M |
| Clinic — appointment booking accepts past-dated times (no past-time guard) | ✅ **Done (Steward, 2026-06-27, 04ef20e, CI green).** `CreateAppointment` + `RescheduleAppointment` now reject a `startsAt` at or before `op.submittedAt` (`ScheduleInPast`) via a new `enforce_future` Starlark guard — a **soft** guard (submittedAt is caller-supplied; the host clock is not exposed to Starlark), appropriate to the trusted single-identity model (Capability-KV §06, the op's own Starlark logic; no frozen contract). The clinic-app booking + reschedule datetime inputs gained a `min` (refreshed on focus) and `friendlyBookingRejection` maps `ScheduleInPast` as a first line of defence. `TestClinic_PastTimeRejected` drives the path (create + reschedule; pre-anchor + at-anchor → rejected, future → accepted) end-to-end through the real Processor. Both clinic integration suites pin the envelope `submittedAt` to a fixed 2026-01-01 anchor so their fixed future-dated bookings stay valid under the guard regardless of wall clock (closing a latent time-bomb the guard would otherwise introduce). Thorough lead review (small, well-scoped, green follow-up; no security plane / no contract). All headless gates green (build / vet / golangci-lint / lint-conventions STRICT / clinic-domain + clinic-reminders + clinic-app tests). In-browser FE verify deferred (F-004 — the shared :7799 stack is a stale bootstrap v10 this run didn't start; a DDL change won't hot-reload + the FE change is a `min` attr + one string-map branch, node --check clean). **Original filing:** `CreateAppointment` (and `RescheduleAppointment`) accept an appointment whose `startsAt` is in the **past** — verified live: booked Dr. Live Hours for **2026-06-22 10:00** (a Monday ~5 days before today 2026-06-27, inside the Mon 09–17 window) → **committed** (`vtx.appointment.WT9Sf…`, since tombstoned). Consequence beyond the obviously-wrong UX: `clinic-reminders` derives `remindAt = startsAt − 24h`, which for a past booking is *also* past, so the `@at` reminder **silently never fires** (no error, no surface). The op validates `startsAt < endsAt`, provider `.hours`, and double-book — but nothing rejects a past time. **Not a platform gap:** the op script already sees `op.submittedAt` (`internal/processor/starlark_runner.go:499`, caller-supplied — fine as a *soft* guard for the trusted-tool single-identity model; the host clock is intentionally not exposed, `starlark_runner.go:76`). Needs: (pkg) reject `startsAt <= op.submittedAt` in CreateAppointment/RescheduleAppointment (a `ScheduleInPast`/`InvalidArgument`, mirroring the existing `endsAt>startsAt` check), and (FE) a `min` on the booking datetime input as a first line of defence. (A deliberate "record a past visit" / back-dated-walk-in path could be added later behind an explicit flag — but the default must not silently accept a future-appointment-in-the-past.) | Clinic | pkg + FE | ★★ | S |
| Clinic — provider availability has no date-specific exceptions (time-off / holidays) | ✅ **Package primitive DONE (Steward, 2026-06-27, 3c3325b).** New opt-in **`.timeOff`** aspect (class `providerTimeOff`) = `{ranges:[{from,to,reason?}]}` (canonical-UTC RFC3339), written by a new **`SetProviderTimeOff`** op (granted operator; validates from/to RFC3339 + from<to, normalizes, ranges=[] clears). `CreateAppointment` + `RescheduleAppointment` now also call **`enforce_time_off`** — an on-demand `kv.Read` (§2.5, non-OCC config read, mirroring `.hours`) of the provider's `.timeOff`, rejecting a booking whose half-open `[start,end)` overlaps any blocked `[from,to)` range (**`ProviderUnavailable`**) **even inside the weekly `.hours`** (a booking must satisfy BOTH layers; back-to-back at a boundary is allowed). Opt-in / backward-compatible (absent `.timeOff` or `ranges=[]` → unrestricted). No frozen contract (Cap-KV §06 — the op's own Starlark logic), no platform/scan seam (the precedent the row predicted). `TestClinic_ProviderTimeOffEnforced` drives the full path through the **real Processor** (overlap rejected; both boundaries accepted; outside accepted; reschedule-into-blackout rejected; no-`.timeOff` provider unrestricted; `ranges=[]` clears; range validation). Package + `verify-package-clinic-domain` assertions extended (10 ops, 7 aspectType DDLs); clinic-domain 0.1.0→0.2.0. clinic-app `friendlyBookingRejection` maps `ProviderUnavailable` to a readable message. Thorough lead review (small, well-scoped, green; follows the `.hours` precedent exactly; no security/contract plane). All headless gates green; verify-package runs fresh in CI (the shared :4222 stack is bootstrap v10 / clinic 0.1.0 and pkgmgr won't hot-upgrade, F-004). ✅ **FE increment DONE (Steward, 2026-06-27, 0b1dc19):** the provider **time-off MANAGER UI** — the `clinicProviders` lens now projects `pr.timeOff.data.ranges AS timeOff` (P5-clean, additive **non-scalar** array column, null when no blackouts; the cypher test proves the engine returns the array + the read model stores it as JSON), so a "🌴 Manage time-off" editor (**read-modify-write**, seeded from the projected ranges, scoped per provider) lists/adds/removes blackout ranges (From / To-inclusive whole-day + optional reason → half-open `[from,(to+1day))` UTC) via `SetProviderTimeOff`, + a soft booking-picker blackout warning under Date & time (the op's `ProviderUnavailable` stays the authority). clinic-domain 0.2.0→0.3.0 (lens spec changed → upgrade reloads it). Thorough lead review; all headless gates green (build/vet/golangci-lint/lint-conventions STRICT/clinic-domain+clinic-app tests/node --check). In-browser verify deferred (F-004 — shared :7799 is a stale binary this run didn't start; lens DDL won't hot-reload). **Remaining = suppress blocked dates in the booking *picker* itself (a calendar-disable, vs. the current soft post-pick warning) — a later polish increment.** **Original filing 2026-06-27 (PO discovery, grounded in code + the live `.hours` model):** Provider availability is **recurring-weekly only**: the `.hours` aspect is `{windows:[{day 0–6, openSec, closeSec}]}` (verified live — Dr. Live Hours = `day:1` Mon 09:00–17:00), enforced by `enforce_hours` in the appointment DDL. There is **no way to block a specific date** — a vacation day, a public holiday, a half-day, a one-off "out sick." A provider who is out on a given Monday still **accepts bookings** that Monday because the weekly window says Mon is open. This is the missing exception layer on top of the recurring schedule (a standard scheduling capability, and **not PHI** — purely operational). **Not a platform gap:** mirror the existing opt-in mechanism — add a `.timeOff`/`.exceptions` aspect (a list of blocked `[from,to]` UTC ranges) set by a new `SetProviderTimeOff` op, read on demand in `enforce_hours` via `kv.Read` (the same Cap-KV §06 "operation's own Starlark logic" + opt-in-aspect pattern `.hours` already uses), rejecting an `OutsideHours`/`ProviderUnavailable` booking that overlaps a blocked range. (FE) a provider time-off manager + suppressing those dates in the booking picker. Distinct from the open "clinic-admin slot & availability management" remaining item (that is FE mgmt of the *recurring* `.hours`; this is the *date-specific exception* primitive that does not exist yet). | Clinic | pkg + FE | ★★ | M |
| Clinic — no encounter / visit documentation (the clinical record is absent) | 🚧 **Filed 2026-06-27 (PO discovery, grounded). `blocked-on:` [lattice.md Privacy/Vault → "Vault + crypto-shredding" (★★★, L)] for the user-facing half.** A **completed visit produces no clinical record.** The appointment carries only `.schedule` (the booking `reason`/chief-complaint) and `.status` (`{value, note}` — the note is just a cancel/no-show *audit* reason); there is **no encounter aspect** (`grep`: no visit/diagnosis/encounter aspect on the appointment beyond those two). So after `SetAppointmentStatus → completed`, nothing captures what happened — assessment, visit summary, diagnosis, follow-up, orders. For a clinic this is *the* core clinical artifact, and its absence is the sharpest **Vault forcing function** (sharper than the generic "Clinic = PHI" observation already folded). **Honest scoping (no-paper-over):** the *recording* op (write an `.encounter` aspect on the appointment) is package-buildable now and would mirror the established `.demographics` pattern (PHI lives in a Core-KV aspect, plaintext-for-now) — **but the user-facing value (projecting + displaying a clinical note) is `blocked-on` the deferred Vault plane**, because the same discipline that keeps `.demographics` *name-only* out of read models (clinicPatients lens comment: "DOB / email / phone are the PHI the deferred Vault plane owns and are intentionally NOT projected") applies to clinical notes. Don't project clinical PHI into a plaintext read model to "ship" this — that papers over the missing Vault. File the recording op as package demand; gate the lens/FE on Vault. | Clinic | pkg + FE | ★★★ | M |
| LoftSpace — landlord decides BLIND (the application captures no applicant qualification profile) | ✅ **Increment 1 DONE (Steward, 2026-06-27, bee9533, CI green) — capture the profile + project derived landlord signals.** New operator-granted **`SetApplicantProfile{leaseAppKey, unit, annualIncome, employmentStatus, employerName?, references?, hasCoApplicant?, hasGuarantor?}`** writes a `.profile` aspect (UNCONDITIONED upsert, re-submittable). The **RAW financials** (annualIncome / employerName / reference strings) live in the Core-KV aspect plaintext-for-now (the `.ssn`/`.demographics` discipline — the deferred Vault plane owns encryption + a raw-financial display later) and are **NEVER projected**; the op **DERIVES** the landlord signals (the rule engine has no arithmetic/len): `incomeToRentMet` (gross monthly income ≥ 3× the unit's listing rent, read on demand via `kv.Read` — the clinic `enforce_hours`/Withdraw precedent), `employmentVerified`, `referenceCount`, `hasCoApplicant`, `hasGuarantor`, verifying `unit` is the application's `appliesToUnit` target (UnitMismatch) before reading rent. The `leaseApplicationComplete` lens projects ONLY those derived signals (`profileSubmitted` + the five) as read-only scalars (never the raw figures; feed no gap/`violating` — capture+surface, not a convergence gate; null `.profile` → null columns, convergence untouched). loftspace-app: the **landlord by-unit row** renders a qualification line (income vs 3× rent / employed / N refs / +co-applicant / +guarantor, or "no profile submitted yet" so a blind decision is visible); the **applicant My Applications card** gains a "Qualification profile" form wiring `SetApplicantProfile` (shown while live, hidden once approved). lease-signing 0.7.0→0.8.0. No frozen contract (Cap-KV §06; §10.2 preserved). Thorough lead review; all headless gates + the 106s `test-lease-convergence` e2e green; in-browser deferred (F-004). **Note:** the applicant "update" form starts blank — the app can't pre-fill the raw income (it isn't projected, per the Vault discipline) — an honest consequence, not a bug. **Remaining (later increments):** the **raw-financial display** half (show the landlord actual income/employer figures) shares the clinic-PHI Vault discipline → 🚧 `blocked-on` [lattice.md Vault]; optionally gate convergence on a `missing_profile` (Increment 1 leaves the profile informational). **Original filing 2026-06-27 (PO discovery, LIVE + source-grounded). The sharpest *product* gap now that the landlord approve/decline surface has shipped.** A landlord can approve/decline an application (`DecideLeaseApplication`, Inc 2 + the landlord FE Inc 3) — but the disposition surface carries **zero applicant qualification data** to decide on. Verified: the `unit_applications.go` DTO exposes only `{ApplicantName, Status, Signed, Approved, Declined, Qualified, LandlordApproved/Declined}`; `CreateLeaseApplication` captures only `{moveInDate, leaseTermMonths, requestedRent}` (the `.terms` aspect); onboarding (`missing_onboarding`) is a bare **SSN-present** check (`leaseApplicationComplete` lens reads `id.ssn.data.value` only for `ssnVal = null`, never projecting the value). So **"Qualified" is a black box** = fake bgcheck + fake payment externalTasks completed + an SSN recorded — there is **no income, employment, rental history, references, credit, co-applicant, or guarantor** anywhere in the package or the surface. A landlord choosing among applicants (the whole point of the two-sided vertical that just shipped) has nothing to look at. The platform pattern already exists and even anticipates this: `loftspace-domain/package.go:27` + `ddls.go:40` comment *"Applicant income / employment is the sensitive data"* (the `sensitiveAspectScope` is designed for it), and the SSN already demonstrates the **capture-in-Core-KV-aspect, gate-on-presence, don't-project-the-value** discipline. **Honest scoping (no-paper-over):** capture income/employment/references/co-applicant/guarantor as application aspects (package-buildable now — a new applicant-profile op + aspect mirroring the SSN/onboarding presence pattern; Cap-KV §06, no frozen contract) and surface a **landlord-decision-supporting view**. A *derived* qualification signal (e.g. income-to-rent ratio met, employment-verified boolean, # references) is projectable + buildable now; the *raw financials* display half shares the clinic-PHI Vault discipline (the SSN-presence-not-value precedent) and that slice may be 🚧 partly `blocked-on` the deferred Vault plane — file the capture + the derived-signal surface now, gate raw-financial display on Vault. Distinct from the ✅ "richer application" row (whose increments delivered property/unit/listing + lease *terms*, never the applicant qualification profile) and the ✅ landlord-surface row (delivered the decision *mechanism* + FE, not the data to decide on). | LoftSpace | pkg + FE | ★★★ | M |
| LoftSpace — a landlord decline carries no reason (bare "declined" to the applicant) | ✅ **Done (Steward, 2026-06-27, 2475beb, CI green).** `DecideLeaseApplication` now reads an optional free-text `reason` (`optional_string`) and stores it on the `.decision` aspect only when supplied (UNCONDITIONED upsert — re-approving overwrites + clears a prior reason; a reasonless decline writes no key); the `leaseApplicationComplete` lens projects `app.decision.data.reason AS declineReason` (null-safe, added to BodyColumns); the applicant declined banner renders "Application declined: <reason>" (a verification decline carries none), the landlord by-unit row echoes the reason, and the Decline action prompts for one. Mirrors the clinic `note`-on-status precedent exactly. No frozen contract (Cap-KV §06); lease-signing 0.6.0→0.7.0; tests through the real Processor + lens cypher; all headless gates + the 107s `test-lease-convergence` e2e green; in-browser verify deferred (F-004). See the Done log. **Original filing 2026-06-27 (PO discovery, source-grounded):** `DecideLeaseApplication` wrote a `.decision` aspect `{value (approved|declined), decidedAt}` only (verified: `ddls.go:118-119`) — a **decline captured no reason**, the `leaseApplicationComplete` lens projects none (`declined` / `landlordDeclined` are bare booleans), and the applicant's "Application declined" banner shows **no explanation**. For a leasing product a decline reason is basic UX (and arguably a fair-housing record). There is a **direct cross-vertical precedent already shipped**: the clinic `SetAppointmentStatus` gained an optional `note` on `.status` for cancel/no-show reasons, projected via a `statusNote` lens column and rendered 📝 on the card. Mirror it: add an optional `reason` to the `DecideLeaseApplication` `.decision` aspect, project a `declineReason` lens column, render it on the landlord decline action (entry) + the applicant declined banner (display). Cap-KV §06, no frozen contract; small. Pairs with the just-shipped landlord surface. | LoftSpace | pkg + FE | ★★ | S |
| Clinic — tombstoned provider / patient / appointment LINGER in the app (Tombstone is cosmetic in the FE) | 🚧 **Filed 2026-06-27 (PO discovery, LIVE + ground-truthed). `blocked-on:` [lattice.md Read-model/projection → "Full-engine lens re-projects a tombstoned vertex when its keyed aspect survives" (★★, S–M)].** A soft-deleted clinic entity stays in the app indefinitely. Verified live on the shared stack: created a throwaway provider (Dr. PO Probe2) + two appointments, then `TombstoneProvider` / `TombstoneAppointment` (both **committed**, `isDeleted:true` confirmed in Core KV via Loupe `/api/vertex`) — yet the provider stayed in `/api/providers` (so it's still **pickable when booking**) and the appointment stayed `scheduled` in `/api/appointments`. So `TombstonePatient` / `TombstoneProvider` / `TombstoneAppointment` are effectively **cosmetic**: nothing a clinic user deletes ever disappears from the roster / schedule / booking picker. **No-paper-over:** the package **cannot** robustly self-fix this — the clinic lenses key on a surviving aspect (`MATCH (pr:provider) WHERE pr.profile.data.fullName <> null`; the root tombstone doesn't cascade to `.profile`/`.demographics`/`.schedule`), no lens in the repo filters `isDeleted`, the **op path** already compensates the same hazard but the **read/lens path has no equivalent**, and a P5-clean app can't filter dead rows (lens read models carry no liveness field; reading Core KV to learn it = P5 violation). So the real fix is the platform full-engine seam (the lattice.md row). Once that lands, re-verify the clinic FE drops tombstoned entities and (optionally) add a delete affordance to the FE (today there is no in-app delete button — `TombstoneX` is CLI/`/api/op`-only, which is itself an FE gap to close after the platform fix). | Clinic | platform (Refractor) + FE | ★★ | S |
| Clinic — patient-side double-booking is UNGUARDED (provider double-book has no patient analog) | **Filed 2026-06-27 (PO discovery, LIVE-confirmed). Buildable now — NOT a platform gap.** The double-book guard is **per-provider only**: `CreateAppointment`/`RescheduleAppointment` read the *provider's* `.bookings` index and reject an overlap (`SlotConflict`), but there is **no patient-side check** — a patient can be booked with **two different providers at the same instant**. Verified live: booked Pat Live with Provider A (Dr. Live Hours) and Provider B (a throwaway) at the **identical** 2026-07-06 14:00–14:30 slot → **both committed** (since tombstoned). A real clinic must not let one patient be in two rooms at once. **Honest scoping:** mirror the proven provider-`.bookings` mechanism with a **patient-side bookings index** (a `vtx.patient.<id>.bookings` aspect appended by CreateAppointment, OCC-snapshotted, `kv.Read` the patient's other live appointments and reject a `PatientDoubleBook` on overlap) — exactly the Cap-KV §06 "operation's own Starlark logic" pattern the provider guard already uses; **no frozen contract, no platform seam**. (FE surfaces the friendly rejection via the existing `friendlyBookingRejection` map.) Distinct from the ✅ provider double-book row. | Clinic | pkg + FE | ★★ | S–M |

**Refractor stale-stack non-projection — ✅ investigated, NOT a code bug (Steward, 2026-06-25, while live-verifying Increment C).** On a *long-lived accumulated* `up-full` stack a freshly-created application never projected into `leaseApplicationComplete` and the `my-tasks` bucket stayed empty (despite 18 live task vertices) — the Refractor's CDC consumer wasn't advancing. **Decisive clean-stack repro** (`make down` → fresh `make up-full` → `install-loftspace` → one `CreateLeaseApplication`): the application **projected immediately** (`/api/applications`=1, refractor actively logging) and Weaver scheduled the gap-dispatch timers. So the read-path is healthy on a clean deployment; the staleness was an **environmental artifact of the accumulated/many-times-restarted dev stack** (consumer-position / orphaned-state), not a Refractor reliability bug. No action — flag cleared. (Operational note for future live-verify: prefer a fresh `make down`+`up-full` over a re-used long-lived stack.)

**Observations (low priority — folded, not filed as rows):**
- **LoftSpace Documents tab has no aggregated "all my documents" view (LoftSpace run, 2026-06-26).** `GET /api/objects?applicant=<key>` matches `ownerKey` **exactly** (verified live: a doc attached to `vtx.leaseapp.<id>` returns under `?applicant=<that leaseapp>` but `count:0` under `?applicant=<the owning identity>`). So a document lives only under the precise record scope it was attached to; there is no roll-up across an applicant's identity + all their leaseapps. The FE's `doc-scope` dropdown mitigates (you switch scope per record), so it's a UX nit, not a blocker — folded, not filed. A pure-FE fix would union the applicant's known owner keys (identity + their leaseapps, which the FE already lists) into one view. (Also confirmed live during this run: object upload → `objectAttachments` lens projection has a ~1–2s lag — the first `?applicant=` read after upload returned `count:0`, the retry returned the doc; expected Refractor projection latency, not a bug.)
- **Clinic = PHI → it's the demand driver for the deferred Vault / crypto-shred plane.** Patient records (DOB, SSN, diagnoses, medical history) are exactly the sensitive-aspect + right-to-be-forgotten case the *Vault + crypto-shredding* item (Deferred → Privacy/Vault) was specified for. Not refiling the enabler — flagging that the clinic vertical is its forcing function (per agentic-ops-design §5: demand pulls deferred platform work into existence). Whoever schedules the Vault work should treat clinic patient-record deletion as the validating flow.
- **No vertical (LoftSpace *or* clinic) is installed by `make up-full`** — already noted for LoftSpace below; clinic isn't even packaged yet (the `clinic-domain` row above is the prerequisite). A per-vertical opt-in install (`make up-clinic`) would unblock this PO loop ever exercising clinic *live* rather than statically.
- **Operator can't see *open* gaps per anchor.** `lattice weaver list` (and Loupe by extension) shows the target's *declared* gap set, not which gaps are actually open right now — after signing, it still listed all four. "What is this application blocked on?" is unanswerable from the operator surface. Largely subsumed by the planned *Loupe system-map / agent-activity console* (★★★, Refinements & ops) — flagging the per-anchor live-gap-state requirement for whoever builds it.
- **`make up-full` does not install the LoftSpace vertical** (only rbac / identity / objects-base). ✅ **Resolved** (32bb340): `make install-loftspace` installs orchestration-base → service-domain → lease-signing onto a running `up-full`, in dependency order (verified live). `up-full` stays core-only by design; the vertical is opt-in.
- **A malformed anchor vertex pins a Weaver target into permanent `error` health (Clinic run, 2026-06-26).** On the shared stack, `health.weaver` was red with two repeating `TemplateDataError`s: `target leaseApplicationComplete gap missing_onboarding / missing_payment: param "subject" references row.applicant, which is null/absent in the row`. Root cause is **LoftSpace**, not clinic — a bare/partial `vtx.leaseapp` shell (root `{}`, no `applicant`) projects a convergence-lens row with `applicant=null`, and the gap's dispatch template can't resolve `subject` → Weaver re-errors every reconcile and never clears. This is the downstream *symptom* of the already-filed **LoftSpace concurrent/duplicate-application guard** gap (a second bare application committed). Worth noting as a small **platform robustness** seam for whoever takes that item: Weaver should **skip + surface a single bad row** (degraded, not a red-forever target) rather than re-emitting an error per reconcile on an un-dispatchable row. No new row — folded into the existing LoftSpace lifecycle/guard item.

### PO notes (dated — drives rotation)

- **2026-06-24 — LoftSpace (first PO run).** Brought up `make up-full` (clean), but it omits the vertical
  → manually installed orchestration-base + service-domain + lease-signing on top. Drove the real
  lease-application flow end-to-end via the `lattice` CLI: created an applicant identity →
  `CreateLeaseApplication` → watched convergence auto-dispatch via Weaver + Loom. Confirmed the
  background-check + payment externalTasks ran through the bridge (fake adapters) and wrote `completed`
  outcomes (with `validUntil` freshness), and two human userTasks opened (`RecordIdentityPII`,
  `SignLease`). Drove `SignLease` to close `missing_signature`. Findings above are all **verified against
  the live stack**, not static analysis. Stack torn down (`make down`). **Next run rotates to Clinic**
  (forcing-function vertical — currently exists only in design docs, so expect a static
  capability/product-gap pass: what a non-leasing domain needs that the platform/packages don't yet
  provide).
- **2026-06-24 — Clinic (first PO run, static).** As predicted, **no clinic package exists** (`ls
  packages/` → identity/lease/location/objects/orchestration/rbac/service; none clinic), so this was a
  static capability/product-gap pass — did **not** bring up a stack (`make up-full` carries no clinic
  vertical to drive). Grounded every finding in code/contracts, not speculation: confirmed (a) `@every`
  recurring scheduling has **no consumer** — substrate enables it (`AllowMsgSchedules`, primordial.go:198)
  but bridge + Weaver temporal lane are all `@at` one-shot (§10.4); (b) temporal availability / double-book
  rejection is **explicitly deferred** by Capability-KV §06 (L354–359) to a "Phase 2 mechanism"; (c) PHI =
  the demand driver for the deferred Vault/crypto-shred plane. Filed 4 clinic rows (domain pkg, `@every`
  forcing function, scheduling-conflict, FE) + 2 observations. The clinic is doing its job as the
  flywheel-validation vertical (agentic-ops-design §5): it pulls `@every` / Vault / temporal-availability
  out of "deferred" and into "demanded." **Next run rotates back to LoftSpace** (now exercisable live —
  re-drive the lease flow and check whether the Steward has burned down any of the 2026-06-24 LoftSpace
  rows, e.g. the decline/manual-review outcome or task-content items).
- **2026-06-26 — LoftSpace (2nd PO run, LIVE).** Layered the LoftSpace vertical onto the already-running
  `up-full` stack (`make install-loftspace` — orchestration-base→location-domain→loftspace-domain→
  service-domain→lease-signing all committed) and started `loftspace-app` on :7788 alongside the running
  clinic-app (:7799) + Loupe (:7777); fresh installs, so the loftspace lenses loaded (no F-004 skip). Drove
  the **full lease-application lifecycle end-to-end through the real Processor** via `/api/op`: minted a unit
  (`CreateLocation`) → `SetListing`/`SetUnitAddress` → created applicant *Priya* (`CreateUnclaimedIdentity`)
  → `CreateLeaseApplication` → watched Weaver/Loom/bridge converge (bgcheck + payment auto-completed by the
  fakes; two human userTasks `RecordIdentityPII` + `SignLease` opened) → completed both bound ops + closed
  both tasks (`CompleteTask` with `reads:[taskKey]`+`class:task`) → application **fully converged**
  (`violating:false`) and the inbox emptied (open tasks 2→0). **Confirmed healthy:** listings/identities/
  applications/tasks read paths all project P5-cleanly; the userTask-dedup fix holds (exactly 2 tasks, no
  duplicates). **Note for future PO runs:** `CompleteTask` (and the read-dependent ops) require the target
  key in `reads`/`contextHint.reads` — a manual curl that omits them returns `UnknownTask`; the FE gets this
  right. **Filed 3 NEW grounded gaps** (all verified live, none on the board): the missing **landlord/
  property-manager surface** (★★★ — the vertical is applicant-only; no post-listing, no per-unit applicant
  view, no human decision), **in-app applicant onboarding** (★★ — no "new applicant" affordance; clinic-app
  has "New patient", loftspace-app doesn't), and **unit/listing lifecycle** (★★ — an approved unit stays
  `available` + accepts duplicate concurrent applications; no analog to clinic's op-time double-book guard).
  The meta-insight: the applicant FE is complete and solid, but LoftSpace is still a **one-sided demo** —
  the landlord side + the unit/listing lifecycle are the next product frontier. Stack left **up** for Andrew
  (Loupe :7777 · loftspace-app :7788 · clinic-app :7799). **Next run rotates to Clinic.**
- **2026-06-26 — Clinic (1st PO run, LIVE — first time clinic exists as real code).** Reused the already-up
  shared stack (NATS :4222 · Loupe :7777 · clinic-app :7799 · loftspace-app :7788) — no `up-full`, no
  `make down`. Clinic was already installed (Dr. Live Hours w/ Mon/Wed 09–17 hours · Pat Live · one
  appointment). Drove the **scheduling domain end-to-end through `clinic-app`'s `/api/op` + read APIs**, all
  against the real Processor: (a) **double-book** overlap 10:15–10:45 onto an existing 10:00–10:30 →
  `SlotConflict` ✓; (b) **out-of-hours** Mon 20:00 → `OutsideHours (weekday 1, 72000–73800s)` ✓; (c)
  **out-of-day** Sun 10:00 → `OutsideHours (weekday 0)` ✓; (d) valid adjacent 10:30–11:00 → **committed** ✓;
  (e) `SetAppointmentStatus → confirmed` → projected `confirmed` via the lens ✓. **The package/platform half
  (conflict + availability enforcement) is solid and the FE surfaces friendly rejections** (`friendlyBookingRejection`
  maps SlotConflict/OutsideHours). **Filed 1 NEW grounded gap:** the **appointment-lifecycle FE gap** (★★★) —
  the day-of-visit core (confirm · check-in · complete · no-show) is unreachable from the FE (only Cancel +
  Reschedule), though the op, the lens `status` column, and the status badges all already support the full
  set; pure FE + a small `reason`-on-status package follow-on. Also folded a **platform-robustness observation**:
  a malformed LoftSpace `leaseapp` anchor (null `applicant`) had pinned `health.weaver` red with repeating
  `TemplateDataError`s — the symptom of the already-filed LoftSpace duplicate-application gap; Weaver should
  skip+surface a bad row, not error-forever. Did **not** refile already-tracked clinic items (slot-picking,
  `@every` recurring, Vault/PHI). Stack left **up** for Andrew. **Next run rotates back to LoftSpace.**
- **2026-06-26 — LoftSpace (3rd PO run, LIVE).** Reused the already-up shared stack (NATS :4222 · Loupe
  :7777 · loftspace-app :7788 · clinic-app :7799) — no `up-full`, no `make down`. The Steward was mid-flight
  on listing-status-on-approval (uncommitted `cmd/loftspace-app/applications.go` + `app.js`), so I steered
  clear of that seam. Drove the applicant flows through `loftspace-app`'s read + `/api/*` paths: browse (1
  unit, `available`) · 2 applicants (Dana, Priya) · Priya's applications (one fully converged `violating:false`,
  one still `missing_signature`) · the **Documents tab end-to-end** (uploaded a proofOfIncome → `AttachObject`
  committed, oid returned → projected into the `objectAttachments` lens after ~1–2s). Confirmed read paths
  project P5-cleanly. **Verified live, then grounded in source:** `SignLease` writes only a `.signature`
  aspect — no lease document is rendered or produced; lease-signing has **no withdraw/cancel op** and the FE
  has no withdraw affordance (only doc-detach). **Filed 2 NEW grounded gaps** (both verified live + in source,
  neither on the board): **no way to withdraw/cancel a lease application** (★★ — the missing complement to the
  just-shipped duplicate-application guard; an applicant who applied wrong is stuck) and **the signed lease is
  content-free** (★★ — no lease to review before signing, no signed artifact produced after; the Documents
  "Signed lease PDF" slot is an orphaned manual upload). Folded one low-priority observation (Documents tab has
  no aggregated per-applicant view; `?applicant=` is exact-ownerKey-match). Did **not** refile the open
  landlord/property-manager surface (★★★) or the in-flight listing-status work. Stack left **up** for Andrew.
  **Next run rotates to Clinic.**
- **2026-06-27 — Clinic (2nd PO run, LIVE).** Reused the already-up shared stack (NATS :4222 · Loupe :7777 ·
  clinic-app :7799 · loftspace-app :7788) — no `up-full`, no `make down`. Clinic already installed (1 provider
  "Dr. Live Hours", `.hours` = Mon 09–17 · 1 patient "Pat Live" · 2 completed appointments). Drove booking via
  `clinic-app`'s `/api/op` + read APIs and **cross-checked ground truth against Core KV through Loupe**
  (`/api/vertex`, `/api/corekv/entry`). Re-verified the shipped scheduling core is **solid** (double-book +
  hours enforcement both reject correctly). **Filed 3 NEW grounded gaps, none on the board:** (1) **past-time
  bookings accepted** (★★, S — LIVE-confirmed: booked a slot ~5 days in the past → committed, and the `@at`
  reminder silently never fires; soft-guardable via `op.submittedAt`, *not* a platform gap), (2) **no provider
  date-specific availability exceptions / time-off** (★★, M — `.hours` is recurring-weekly only; a provider out
  on a Monday still accepts that Monday's bookings; mirror the opt-in `.hours` pattern with a `.timeOff`
  aspect), (3) **no encounter / visit documentation** (★★★, M — a completed visit produces no clinical record;
  the recording op is package-buildable but the display half is 🚧 **blocked-on** the Vault plane in
  `lattice.md` — the sharpest Vault forcing function). Confirmed the deliberate **PHI name-only** lens
  discipline (demographics captured at create but never projected/editable, *by design* pending Vault) —
  folded, not refiled (it IS the Vault item). **Operator footgun re-confirmed (folded, not filed):** clinic-app's
  `/api/op` takes **top-level `reads`** while loftspace-app takes **`contextHint.reads`** — a manual curl with the
  wrong shape returns a misleading `UnknownEndpoint: … absent or tombstoned` even though the vertex is alive (the
  FEs get the shape right; only manual probing hits it). Created + then **tombstoned** one throwaway past-dated
  appointment during probing (left the shared stack clean). Did **not** refile tracked clinic items (slot-picking,
  `@every` recurring, Vault/PHI). Stack left **up** for Andrew. **Next run rotates to LoftSpace.**
- **2026-06-27 — LoftSpace (4th PO run, LIVE + source-grounded).** Reused the already-up shared stack (NATS
  :4222 · Loupe :7777 · loftspace-app :7788 · clinic-app :7799) — no `up-full`, no `make down`. Drove the
  applicant read paths through `loftspace-app`'s `/api/*`: 1 unit (Portland loft, `available`) · 2 applicants
  (Dana, Priya, both `unclaimed`) · 2 of Priya's lease applications on the same unit (one converged
  `violating:false`, one `missing_signature`) · 1 open `SignLease` task. **F-004 confirmed live:** the shared
  :7788 binary is **stale** — `/api/unit-applications` (the shipped landlord-surface data layer, server.go:44)
  returns **404**, so the landlord FE could not be exercised live (binary predates Inc 1–3); did **not**
  restart it (not this run's process — precedent). So this was a read-paths-live + **source-grounded** pass on
  current code. **Filed 2 NEW grounded gaps, neither on the board, both sharpened by the now-shipped landlord
  surface:** (1) **landlord decides BLIND** (★★★, M) — the disposition DTO + `CreateLeaseApplication` capture
  ZERO applicant qualification data (no income/employment/references/co-applicant/guarantor); "Qualified" =
  fake bgcheck + fake payment + an SSN-present check (`leaseApplicationComplete` reads `id.ssn` for presence
  only). The `loftspace-domain` PII-scope machinery already anticipates "applicant income/employment is the
  sensitive data" — capture + a derived qualification signal are buildable now; raw-financial display shares
  the clinic-PHI Vault discipline (partly 🚧 Vault). (2) **a landlord decline carries no reason** (★★, S) —
  `DecideLeaseApplication` writes `.decision {value, decidedAt}` only; the applicant sees a bare "declined";
  mirror the just-shipped clinic `note`-on-status precedent (optional `reason` → `declineReason` lens column →
  surface). **Folded (not filed):** the 2 coexisting same-applicant/same-unit leaseapps for Priya are
  **pre-guard stale data** (the duplicate guard checks the unit's `.leaseApplications` index, which those
  pre-2026-06-26 apps were never written into) — not a guard bug; no clean live repro on the stale stack, so
  not probed (would only pollute). Did **not** refile tracked rows (lease-doc Inc B, withdraw, listing-status,
  the landlord surface). Stack left **up** for Andrew. **Next run rotates to Clinic.**
- **2026-06-27 — Clinic (3rd PO run, LIVE + ground-truthed).** Reused the already-up shared stack (NATS :4222 ·
  Loupe :7777 · clinic-app :7799 · loftspace-app :7788) — no `up-full`, no `make down`. Clinic already installed
  (1 provider "Dr. Live Hours" Mon/Wed 09–17 · 1 patient "Pat Live" · existing appointments incl. a stale
  past-dated `WT9Sf…` "scheduled" from a prior run — pre-past-time-guard data, left alone). Drove booking +
  tombstone flows through `clinic-app`'s `/api/op` + read APIs and **cross-checked ground truth in Core KV via
  Loupe** (`/api/vertex`). **Filed 2 NEW grounded gaps, neither on the board, both rooted in live evidence:**
  (1) **tombstoned provider/patient/appointment LINGER** (★★, S — created a throwaway provider + 2 appts,
  tombstoned all three: each `isDeleted:true` in Core KV yet still in `/api/providers` / `/api/appointments`;
  `TombstoneX` is cosmetic in the FE) → **routed the platform half to `lattice.md`** (a Refractor full-engine seam:
  the aspect-presence lens re-projects a tombstoned anchor because the CDC anchor node bypasses `fetchNode`'s
  `isDeleted` filter, while the op path already compensates via the documented vertex-`isDeleted` gate) and marked
  the vertical item **🚧 blocked-on** it (no-paper-over: the package + a P5-clean app genuinely can't fix it
  read-side). (2) **patient-side double-booking is unguarded** (★★, S–M — booked Pat Live with **two providers at
  the identical 14:00 slot**, both committed; the `.bookings` double-book guard is per-provider only) — **buildable
  now**, mirror the provider mechanism with a patient `.bookings` index (Cap-KV §06, no frozen contract, no platform
  seam). **Confirmed solid / not refiled:** the provider-side double-book + `.hours` + past-time + time-off guards
  all still reject correctly; reminders surface (`reminderSentAt` → 🔔); the PHI name-only lens discipline holds
  (it IS the Vault item). **Cleanup:** tombstoned all 3 of my throwaway probes (the lingering-roster bug means the
  tombstoned provider still shows — that's finding (1), not residue). Stack left **up** for Andrew. **Next run
  rotates to LoftSpace.**

---

## Done log — verticals (newest first)

_Append shipped Stream-1 items here (this lane only — keeps the board collision-safe)._

- **2026-06-27 — Clinic: name why a date has no open slots in the booking picker (fff351d, CI green).** Advances the "Clinic FE" row's remaining **blocked-dates-in-the-picker** item. After the slot picker shipped (ecad67c), a date with no slots showed one generic line ("No open slots that day — try another date or a shorter duration") for *every* reason — masking the two cases a booker most needs named: the provider doesn't work that weekday, or is on time-off that whole day. New `noSlotsReason(p, dateStr)` (pure FE, `cmd/clinic-app/web/app.js`) returns a specific message — **weekday-not-worked** → `"<name> doesn't see patients on Sundays — available Mon, Wed."` (lists the provider's actual working days) · **whole-day time-off** → `"<name> is on time-off that day (Vacation) — pick another date."` (with the authored reason) · else `""` so the caller keeps the generic booked/duration line. Mirrors `computeOpenSlots`' UTC-day interpretation exactly (`dateStr+"T00:00:00Z"`, `getUTCDay`, `Date.parse` on `.timeOff` from/to) so the reason matches the slots that would render; a date is "on time-off" when the full UTC day falls inside one whole-day-authored `[from,(to+1day))` range. `textContent` (no XSS); picker stays advisory, the op remains the booking authority; no slot-computation/submit/op change. No frozen contract / no lens / no Go-logic / no security plane (pure client-side JS). Thorough lead review (UTC parity with computeOpenSlots, time-off coverage math, weekday-list, null-safety, XSS — walked; small well-scoped green follow-up). Verified: `node --check` clean; the cycled `bin/clinic-app` serves the new asset (HTTP 200, `noSlotsReason` present) bound to the live core stack; a node harness against the live "Dr. Live Hours" provider shape (Mon/Wed 09–17, a whole-day vacation) asserts all three branches produce the exact intended strings. All headless gates green (build / vet / golangci-lint / lint-conventions STRICT / clinic-domain + clinic-app tests / node --check). **Remaining = actually *graying-out* blocked dates in the native date input (needs a custom calendar — `<input type=date>` can't exclude arbitrary dates) + clinic-admin availability mgmt.**

- **2026-06-27 — Clinic: availability-aware booking slot picker (ecad67c, CI green).** Advances the "Clinic FE — patient booking + provider schedule" row's remaining **available-slot picking** item (★★★) — the booking form was free datetime entry with a soft post-pick time-off warning; now it suggests the provider's actual open slots. **Lens:** the `clinicProviders` spec projects `pr.hours.data.windows AS hours` (non-scalar array column, mirroring the proven `timeOff` projection — null when the provider has set no windows; clinic-domain 0.3.0→0.4.0, manifest+Package synced). **Go:** `providerRow` gains `Hours []hoursWindow` (decoded from the lens JSON). **FE:** a Book-form **"Find available times"** picker (a date input) renders clickable slot buttons computed for the chosen date from `.hours` − `.timeOff` − the provider's existing appointments − past times, at the chosen duration's granularity; clicking a slot fills the Date & time field. Slots are generated in UTC seconds-of-day (matching how `.hours` is keyed) so a suggested slot passes the op's `enforce_hours`; the time-off / double-book / past-time exclusions mirror `enforce_time_off` / the `.bookings` double-book check / `ScheduleInPast`. The op stays the authority — the picker is advisory (a cancelled/no-show appointment is excluded from the block set, matching the op; a provider with no `.hours` shows "enter a date & time directly"; the per-provider appointment fetch is cached and invalidated on a successful booking). No frozen contract (Cap-KV §06; the lens is package work). Thorough lead review (UTC slot↔op-enforcement parity, the click→`#startsAt` round-trip back to the same UTC instant on submit, the async-fetch race re-check, cache invalidation, no-XSS computed labels — all walked; well-scoped additive FE + tiny lens/Go change, no security/contract plane). Tests: `TestClinicProviders_ProjectsHoursWindows` (real `full` engine — hours array projects, null when absent), `TestComputeProviders_SortsAndSkips` extended (Hours round-trips). All headless gates green (build / vet / golangci-lint / lint-conventions STRICT / clinic-domain + clinic-app tests / node --check); `verify-package-clinic-domain` runs fresh in CI (not run locally — it would upgrade the shared :4222 stack, F-004). In-browser verify deferred (F-004 — lens change won't hot-reload + the shared :7799 binary is stale, not started by this run). **Remaining = disable past/blocked dates in the date *picker* itself + clinic-admin slot/availability management (later polish).**

- **2026-06-27 — LoftSpace: applicant qualification profile + derived landlord signals — Increment 1 (bee9533, CI green).** Advances the "landlord decides BLIND" row. The landlord could approve/decline but had nothing to decide on — `CreateLeaseApplication` + the disposition DTO captured zero qualification data ("Qualified" = fake bgcheck + fake payment + SSN-present). New operator-granted **`SetApplicantProfile{leaseAppKey, unit, annualIncome, employmentStatus, employerName?, references?, hasCoApplicant?, hasGuarantor?}`** on the leaseapp DDL writes a `.profile` aspect (UNCONDITIONED upsert, re-submittable). **No-paper-over / Vault discipline:** the RAW financials (annualIncome, employerName, the reference strings) live in the Core-KV aspect plaintext-for-now (the `.ssn`/`.demographics` pattern — the deferred Vault plane owns encryption + a raw-financial display) and are **NEVER projected**; the op **derives** the landlord-facing signals (the full engine has no arithmetic/`len`) — `incomeToRentMet` (gross monthly ≥ 3× the unit's listing rent, read on demand via `kv.Read`, verifying `unit` is the application's `appliesToUnit` target → UnitMismatch), `employmentVerified`, `referenceCount`, `hasCoApplicant`, `hasGuarantor` — and the `leaseApplicationComplete` lens projects ONLY those (+`profileSubmitted`) as read-only scalars, never the raw figures. They feed no gap/`violating` (capture+surface, not a convergence gate), so a null `.profile` degrades to null columns and convergence is untouched (proven: one-row-per-anchor + the 106s convergence e2e). loftspace-app: the landlord by-unit row renders a qualification line (income vs 3× rent / employed / N refs / +co-applicant / +guarantor, or "no profile submitted yet" so a blind decision is visible); the applicant My Applications card gains a "Qualification profile" form wiring the op (shown while live, hidden once approved). lease-signing 0.7.0→0.8.0. No frozen contract (Cap-KV §06; §10.2 one-row-per-anchor preserved). Thorough lead review (Vault discipline / convergence integrity / UnitMismatch / stored-bool projection / FE-XSS / permission-consistency walked; well-scoped additive follow-up on the Decide/.decision + terms-projection + landlord-FE precedents, no security/contract plane). Tests: `TestSetApplicantProfile` (store / derive incomeToRentMet vs rent / re-submit-overwrites / UnitMismatch) through the real Processor, `TestLeaseApplicationComplete_ProjectsQualificationProfile` (derived projects, raw stays unprojected, graceful null degrade), `TestGroupByUnit_CarriesQualificationProfile`. All headless gates green (build/vet/golangci-lint/lint-conventions STRICT/lease-signing+loftspace-app tests/node --check) + the 106s `test-lease-convergence` e2e. In-browser verify deferred (F-004 — lens change won't hot-reload + the shared :7788 binary is stale, not started by this run). **Remaining = the raw-financial DISPLAY half (🚧 blocked-on the Vault plane) + an optional `missing_profile` convergence gate.**
- **2026-06-27 — LoftSpace: capture an optional reason on a landlord decline (declineReason) (2475beb, CI green).** Closes the "a landlord decline carries no reason" row. `DecideLeaseApplication` wrote only `.decision {value, decidedAt}` — a decline reached the applicant as a bare "Application declined." Mirrored the shipped clinic `note`-on-status precedent: the op reads an optional free-text `reason` (`optional_string`) and stores it on the `.decision` aspect **only when supplied** (the write stays an UNCONDITIONED upsert, so re-approving overwrites + clears a prior reason; a reasonless decline writes no key). The `leaseApplicationComplete` convergence lens projects `app.decision.data.reason AS declineReason` (null-safe informational column, added to BodyColumns; §10.2 one-row-per-anchor preserved). loftspace-app: `applicationRow` + `applicantSummary` gain `DeclineReason` (both decode the lens JSON directly); the applicant declined banner renders "Application declined: <reason>" for a landlord decline (a verification decline carries none), the landlord by-unit row echoes the recorded reason, and the Decline action prompts for an optional reason. ddls.go: `reason` added to the op input schema + field description + op description + a decline-with-reason example. lease-signing 0.6.0→0.7.0. No frozen contract (Cap-KV §06 — the op's own Starlark logic). Thorough lead review (small, well-scoped, green; direct clinic precedent; no security/contract plane). Tests: `TestDecideLeaseApplication_Reason` (store / clear-on-reapprove / reasonless) through the real Processor + `TestLeaseApplicationComplete_ProjectsDeclineReason` (lens projection + clear). All headless gates green (build / vet / golangci-lint / lint-conventions STRICT / lease-signing + loftspace-app tests / node --check) + the 107s `test-lease-convergence` e2e. In-browser verify deferred (F-004 — the lens change won't hot-reload + the shared :7788 binary is stale, not started by this run).
- **2026-06-27 — LoftSpace signed-lease, Inc B: produce + attach the executed lease on SignLease (426f4eb, CI green).** Completes the signed-lease row (Inc A terms-review panel + Inc B the executed artifact). The applicant signed a lease that existed as no document — `SignLease` wrote only a `.signature` aspect. Now: the `leaseApplicationComplete` lens projects `signedAt` (the execution date) as a read-only scalar (mirrors Inc A's terms columns — feeds no gap; `missing_signature` already derives from `signedAt=null`); a new `GET/POST /api/lease-document` produces the executed lease — GET streams a **deterministic** plain-text summary (parties / premises / terms / execution date) built on demand from the projected terms + the applicant's `applicantRoster` name (P5: lens read models only, no Core KV, no projection-lag dependency), POST durably attaches the **byte-identical** content to the leaseapp under a `signedLease` slot via objects-base (deterministic store name from the leaseAppKey → a re-produce overwrites rather than orphaning; content-derived requestId → idempotent attach; reclaims bytes on a rejected attach, matching the upload handler). FE: a "📄 Signed lease" download link on every signed application card + a best-effort durable attach fired when SignLease commits. lease-signing 0.5.0→0.6.0. No frozen contract (Cap-KV §06; §10.2 one-row-per-anchor preserved). Thorough lead review (walked determinism, idempotent/orphan-free attach, the rejected-attach reclaim, and the signed-only gate; well-scoped projection-add + new endpoint + FE, no security plane). All headless gates green (build / vet / golangci-lint / lint-conventions STRICT / go test loftspace-app + lease-signing incl. the new `signedAt` cypher assertion / node --check) + the 108s `test-lease-convergence` e2e. In-browser verify deferred (F-004 — lens change won't hot-reload + the shared :7788 binary is stale, not started by this run). **The signed-lease row is now fully ✅.**
- **2026-06-27 — Clinic: provider time-off MANAGER UI + booking blackout warning (0b1dc19).** The `SetProviderTimeOff` op + `.timeOff` aspect shipped (3c3325b) but were unreachable from the app — no way to declare a vacation / holiday / out-sick day, and the booking picker gave no hint about a blocked date. Wired the FE half: the `clinicProviders` lens now projects `pr.timeOff.data.ranges AS timeOff` (P5-clean, **non-scalar** additive array column, null when none — the cypher test proves the `full` engine returns the array value + the read model stores it as JSON, the first non-scalar lens column in the codebase); `clinic-app providerRow` round-trips it as `TimeOff []timeOffRange`. A "🌴 Manage time-off" editor (**read-modify-write**, seeded from the projected ranges, scoped per provider) lists/adds/removes blackout ranges (From / To-inclusive whole-day + optional reason → stored half-open `[from, (to+1day))` UTC) via `SetProviderTimeOff`; a soft warning under Date & time fires when the chosen start falls inside the selected provider's time-off (same `toRFC3339` conversion the submit uses, so the overlap check matches the op's `enforce_time_off`; `ProviderUnavailable` stays the authority). clinic-domain 0.2.0→0.3.0 (lens spec changed → upgrade reloads it). Thorough lead review (walked the date math, the non-scalar projection round-trip, warning/submit consistency, and the read-modify-write seed — strictly safer than the established write-mostly hours editor); all headless gates green (build/vet/golangci-lint/lint-conventions STRICT/clinic-domain+clinic-app tests/node --check); verify-package runs fresh in CI (F-004). In-browser deferred (shared :7799 is a stale binary this run didn't start; lens DDL won't hot-reload). **Remaining = disable blocked dates in the booking *picker* (vs. the soft post-pick warning) — a later polish.**
- **2026-06-27 — Clinic: provider date-specific time-off exceptions (SetProviderTimeOff / ProviderUnavailable) (3c3325b).** Provider availability was recurring-weekly only (`.hours`) — a provider out on a given Monday still accepted that Monday's bookings; no way to block a vacation week / holiday / out-sick day. Added the date-specific blackout LAYER on top of the recurring hours, mirroring the opt-in `.hours` mechanism (Cap-KV §06, the op's own Starlark logic; no frozen contract, no platform/scan seam): a new opt-in `.timeOff` aspect (class `providerTimeOff`) = `{ranges:[{from,to,reason?}]}` written by a new `SetProviderTimeOff` op, and `enforce_time_off` in `CreateAppointment` + `RescheduleAppointment` (on-demand `kv.Read`, non-OCC config read) rejecting a booking whose half-open `[start,end)` overlaps any blocked range (`ProviderUnavailable`) even inside the weekly `.hours` — a booking must satisfy BOTH layers; back-to-back at a boundary is allowed; absent `.timeOff`/`ranges=[]` → unrestricted (backward-compatible). `TestClinic_ProviderTimeOffEnforced` proves it through the real Processor; package + verify-package assertions extended (10 ops, 7 aspectType DDLs); clinic-domain 0.1.0→0.2.0; clinic-app `friendlyBookingRejection` maps the new code. Thorough lead review (small/well-scoped/green; `.hours` precedent). All headless gates green; verify-package runs fresh in CI (shared stack is v10/0.1.0, pkgmgr won't hot-upgrade — F-004). **Remaining = next FE increment: a provider time-off MANAGER UI + suppress blocked dates in the booking picker (UX→FE, in-browser verify deferred).**
- **2026-06-27 — Clinic: reject past-dated appointment bookings (ScheduleInPast) (04ef20e, CI green).** `CreateAppointment` + `RescheduleAppointment` accepted a `startsAt` in the past (PO-confirmed live: a ~5-day-back slot committed, and its `remindAt = startsAt − 24h` was also past so the clinic-reminders `@at` lane silently never fired). New `enforce_future` Starlark guard rejects a `startsAt` at or before `op.submittedAt` (`ScheduleInPast`) — a **soft** guard (submittedAt is caller-supplied; the host clock is intentionally not exposed to Starlark) for the trusted single-identity model (Capability-KV §06, the op's own Starlark logic; no frozen contract). clinic-app booking + reschedule datetime inputs gained a `min` (refreshed on focus) + a `ScheduleInPast` branch in `friendlyBookingRejection`. `TestClinic_PastTimeRejected` proves the path through the real Processor; both clinic integration suites pin the envelope `submittedAt` to a fixed 2026-01-01 anchor so their fixed future-dated bookings stay valid under the guard regardless of wall clock (closing the time-bomb the guard would otherwise introduce). Thorough lead review (XS–S, well-scoped, green; no security/contract); all headless gates green. In-browser FE verify deferred (F-004 — shared :7799 is a stale bootstrap v10 not started by this run; trivial FE delta, node --check clean).
- **2026-06-26 — LoftSpace signed-lease, Inc A: project lease terms + a terms-review panel (777d180, CI green).** The applicant could sign a lease they never saw — the leaseapp `.terms` aspect was written but unprojected, and the convergence row carried only rent + address line1. The `leaseApplicationComplete` convergence lens now projects the full lease economics as pure read-only scalar columns (the applicant's requested `.terms` moveInDate/leaseTermMonths/requestedRent + the rest of the unit's `.listing` currency/bedrooms/bathrooms/leaseTermMonths/availableFrom + `.address` city/region); none feeds `violating`/a gap, and a missing `.terms` degrades to null columns. loftspace-app's My Applications card renders a **"Lease terms"** review panel (headed *"review before signing"* while the sign step is open) showing property/size/rent (+ the applicant's offered rent when it differs)/term/move-in, only the present fields. No frozen contract (Cap-KV §06; §10.2 one-row-per-anchor preserved); lease-signing 0.4.0→0.5.0. Thorough lead review (well-scoped projection-additions + FE panel); all gates green incl. the 107s `test-lease-convergence` e2e + new cypher assertions for every new column. In-browser deferred (F-004). **Remaining = Inc B: produce + attach a signed-lease artifact on SignLease.** Also corrected two stale board rows: unit/listing-lifecycle Part (a) listing-status-on-approval was already shipped via landlord Inc 2 (the `missing_listingLeased → directOp(SetListingStatus)` mechanism), so that row is now ✅.
- **2026-06-27 — LoftSpace landlord surface, Inc 3: the landlord FE (db4073a, CI green).** Completes the landlord surface (Inc 1 data layer + Inc 2 decision op + Inc 3 FE). A top-bar **Applicant ⇄ Landlord** mode toggle (persisted) swaps the applicant tab strip for a **my-units view** over the shipped `GET /api/unit-applications` (P5: lens read models only). Per-unit cards (address / rent / status / app count) list each application with a disposition badge (qualified / in-review / approved / leased / declined) + signed marker; a **qualified** application gets **Approve / Decline** buttons wiring `DecideLeaseApplication`, and a leased unit shows "leased to another applicant" with actions suppressed. A **Post-a-listing modal** mints + lists a unit via `CreateLocation` → `SetUnitAddress` → `SetListing`. Pure FE, no Go change; all headless gates green + JSON-shape cross-check vs the `unit_applications.go` DTO. In-browser click-through deferred to an attended pass (F-004 — the shared :7788 binary is stale and was not started by this run). Design: `implementation-artifacts/loftspace-landlord-surface-design.md` Inc 3. **The landlord surface is now feature-complete; the vertical is two-sided.**
- **2026-06-26 — LoftSpace landlord surface, Inc 2: `DecideLeaseApplication` + landlord-gated lease (6c30a10, CI green).** The lease no longer auto-leases on applicant readiness — a human landlord decision gates it. New operator-granted `DecideLeaseApplication{leaseAppKey, decision∈{approved,declined}}` op writes a `.decision` aspect (unconditioned upsert → reversible); the `leaseApplicationComplete` convergence lens gains `landlordDecision`/`landlordApproved`/`landlordDeclined`/`missing_decision`; `missing_listingLeased` gates on applicant-readiness AND landlord-approval (Winston ruling, hardened by the 3-layer review which caught that landlord-approval-alone gating would lease to an unqualified/stale applicant + that the FE decision columns never reached the client through the `applicationRow` DTO). loftspace-app applicant banner + landlord by-unit disposition wired. No frozen contract (Cap-KV §06; §10.2 preserved). lease-signing 0.4.0. Design: `implementation-artifacts/loftspace-landlord-surface-design.md` Inc 2. **Next: Inc 3 — the landlord FE (UX→FE).**
