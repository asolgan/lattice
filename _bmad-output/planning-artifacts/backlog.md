# Lattice Backlog & Roadmap (Phase 3+) — index

**Owner:** Andrew (architect / planning lead). **Status:** living document.

Work is tracked in **two swim-lane files**, advanced by **two parallel streams** (split along the
no-collision seam: app-vertical code vs platform code). Design + rationale:
`implementation-artifacts/agentic-ops-swimlanes-design.md`.

- **[backlog/verticals.md](backlog/verticals.md)** — App Verticals (LoftSpace/Clinic packages + FE). Stream:
  **Vertical Steward** + **Vertical PO**.
- **[backlog/lattice.md](backlog/lattice.md)** — Lattice features + component maintenance. Stream: **Lattice
  Steward** (round-robin across components) + **Surveyor**.

This index keeps the **scales**, the **cross-lane rules**, and the **shipped history** (the Progress board
below). Per-lane *ready / in-flight / done* lives in the two lane files (each stream writes only its own file →
the single-file board-collision races go away).

**Scales.** Importance: ★ low · ★★ medium · ★★★ high. Size: XS · S · M · L · XL.
**Status vocabulary.** 📋 ready · 🏗️ in-flight (worktree) · ✅ done (commit) · 📐 design-proposed · 🚧 blocked
(Andrew-gated, or `blocked-on:` another item).

## Cross-lane rules

- **No-paper-over (Andrew, 2026-06-26).** The App-Verticals stream **never** substitutes a workaround for a
  missing Lattice capability and calls the item done. A discovered platform gap is filed to **lattice.md** as
  demand (tagged with the requesting vertical + why) and the vertical item is **`🚧 blocked-on:`** it — or
  ships only its non-gap part. A vertical builds *on* real Lattice capabilities; faking one hides the gap and
  stalls Lattice. (Boundary vs **P5**: a missing **lens** is *not* a Lattice gap — adding it is **package
  work** the vertical does itself; only a missing platform **primitive** — engine / op / substrate /
  orchestration — routes to lattice.md.)
- **The PO drives Lattice demand too.** Exercising the verticals is the primary, grounded source of "what the
  platform actually lacks" — those gaps land in lattice.md, not worked around in verticals.
- **Prioritization.** Each stream picks by **importance × readiness**; the Lattice stream **round-robins across
  components** so none stalls. Reliability/observability **red pre-empts** either stream. The experience layer
  is **not** a forced priority (the apps largely exist).
- **Frozen contracts are prepare-not-skip.** A needed `docs/contracts/*` change is never a reason to skip an
  item: make the edit in `main` **uncommitted** + flag it; Andrew ratifies (commits). Only a *standing* Andrew
  block/shelve is a true leave-it.
- **Code in worktrees; docs in `main`.** Code changes build in an isolated git **worktree** (commit + push to
  main, no PR). The **backlog / lane files, design docs, and contracts** are edited **directly in `main`**
  (contracts uncommitted). Always scoped `git add <paths>`, `git pull --rebase`, detect-reuse stack.

---

## Now — balanced prioritization (experience layer no longer forced)

The initial experience-layer push has largely landed: the vertical apps now **exist** (`cmd/loftspace-app`
`:7788`, `cmd/clinic-app` `:7799`) with real flows, and the Loupe live system-map shipped. So the experience
layer is **no longer a forced top priority** (Andrew, 2026-06-25) — the Steward picks by
**importance × readiness**, balancing the experience layer against reliability / observability, component
coverage, and the **PO-filed demand backlog** (the Vertical PO discovery loop is now live and feeds fresh,
exercised items — see [backlog/verticals.md](backlog/verticals.md)). Flow for any UI/app pick: **PO scopes → Sally designs the UX
→ FE Engineer builds + verifies in-browser → Winston admits.** M/L is fine (risk-bounded L2 + multi-fire).

- **Vertical app front-ends (★★★)** — whatever the Vertical POs (LoftSpace, Clinic) decide their apps should
  do; the apps exist and are exercisable live, so this is now **PO-demand-driven** rather than greenfield. Be
  ambitious — new app capabilities welcome.
- **Loupe operator surfaces** — the live "system map" landing page is ✅ shipped. The **agent-activity console
  is ⏸️ shelved** (Andrew, 2026-06-25 — read-seam options rejected; revisit later). Not an active pick.

---


## Progress board

The single index of what is in flight and what has landed. Everything in the themed tables below
defaults to **📋 Backlog**; when an item is picked up it runs the normal loop (design → review → build →
review → commit) and surfaces here as **🏗️ Active → ✅ Done (commit)**. This board is the *index* —
per-item detail lives in design / story docs + git history, never in agent memory and never in a
`sprint-status` file (house rule).

| Item | Status | Ref |
|---|---|---|
| Loom control plane *(Loupe blocker #1)* | ✅ Done | `implementation-artifacts/loom-control-plane.md` |
| Loupe — view & control app | ✅ Done (v1 stab) | `implementation-artifacts/loupe-v1.md` |
| Large-file / binary handling — **v1a** (attach/read/detach) | ✅ Done (built + 3-layer-reviewed; merged to main) | `implementation-artifacts/large-file-binary-design.md` §1–§18 |
| Large-file / binary handling — **v1b** (GC) | ✅ Done (Option A: objectLiveness lens + Weaver directOp + epoch-CAS + object-store-manager; full Loop A+B convergence e2e green in CI; 3-layer-reviewed; merged to main; contracts #7 §7.2 + #10 §10.8 directOp.reads ratified) | `implementation-artifacts/large-file-binary-design.md` §20 |
| Refractor substrate inner-package migration | ✅ Done (Path B, d420ca4) | `implementation-artifacts/substrate-migration-plan.md` |
| Structured adapter result *(terminal-failure producer)* | ✅ Done (828f24d) | `implementation-artifacts/structured-adapter-result-design.md` |
| Async external-reply | ✅ Done (increments 1–3: 0860fb1, 0f85d45, 3504db6) | `implementation-artifacts/async-reply-design.md` |
| location-domain — spatial base package *(SL.1)* | ✅ Done (ae3a056) | `implementation-artifacts/service-location-design.md` |
| service-location — service-access authZ scheme *(SL.2)* | ✅ Done (e4af07c, 715b14b) | `implementation-artifacts/service-location-design.md` |
| Loupe live system-map *(★★★ experience)* — **data layer** (`/api/systemmap` assembler) | ✅ Done (backend + unit tests; lead-reviewed, gates green) | `implementation-artifacts/loupe-v1.md` (API list) |
| Loupe live system-map *(★★★ experience)* — **FE landing view** | ✅ Done (UX-then-FE: Sally spec → FE Engineer build → Winston in-browser-verified against `make up-full` + lead-reviewed; gates green) | `implementation-artifacts/loupe-system-map-ux.md` |
| Task-content — **`my-tasks` lens self-describing** | ✅ Done (cypher aspect-hops `op.canonicalName`/`op.description` → `operationName`/`operationDescription` on the read-model row; refractor e2e asserts; for all consumers) | `packages/orchestration-base/lenses.go` |
| Loupe task-inbox — **operator data layer** (`GET /api/tasks`) | ✅ Done (all-identity task list + op label resolved from forOperation meta; unit-tested, gates green) | `cmd/loupe/tasks.go` |
| Loupe task-inbox — **FE tab** | ✅ Done (Tasks tab: cards per task with op name/description, assignee, expiry, status filter, "Complete in Submit Op →" deep-link; in-browser-verified against a live task, no console errors) | `cmd/loupe/web/{index.html,app.js,style.css}` |
| LoftSpace property domain — **Increment 1: `loftspace-domain` package** | ✅ Done (the `.listing` + `.address` aspect-type DDLs on location-domain's `vtx.unit` + `SetListing`/`SetUnitAddress` ops on a single `loftspaceListing` vertexType DDL; permissions, manifest, `make verify-package-loftspace-domain` (67 assertions) + unit tests; lead-reviewed, gates green; **live-verified** against the running stack — minted a unit, set listing+address through the real Processor, all three negative guards fire) | `packages/loftspace-domain/` |
| LoftSpace applicant app *(★★★ experience — top priority)* — **UX design** | ✅ Done (Winston-ratified, build-ready proposal — four surfaces: Browse&Apply / My Applications / Tasks / Documents; trusted single-identity-in-view model like Loupe; new `cmd/loftspace-app` on `:7788`; the only platform gaps are two headlessly-unit-testable read assemblers `/api/listings` + `/api/applications`, everything else lifts shipped Loupe handlers; 5-increment FE plan). No contract / architecture gate. | `implementation-artifacts/loftspace-applicant-app-ux.md` |
| LoftSpace applicant app — **FE Increment A: scaffold + Browse&Apply** | ✅ Done — new `cmd/loftspace-app` (binds `127.0.0.1:7788`, trusted admin actor like Loupe): `GET /api/listings` (new tombstone-filtering listing+address assembler, unit-tested) + `GET /api/identities` (applicant switcher) + `POST /api/op` (submit), and the vanilla Browse&Apply FE — listings grid, identity switcher (localStorage-persisted), DDL-aligned apply form submitting `CreateLeaseApplication`. **In-browser verified** against the live stack (browse → apply → real leaseapp committed, green toast); a modal-on-load `[hidden]`-override bug was caught in-browser + fixed. `make run-loftspace-app` added. All gates green; lead-reviewed. **P5 deviation (Andrew, 2026-06-25) — ✅ RESOLVED by Increment A.1 below:** the original `/api/listings` + `/api/identities` Core-KV scans now read a lens projection / are removed, and a P5 lint gate prevents regressions. Increments B (status) → C (tasks) → D (documents) → E (up-full wiring) next. | `cmd/loftspace-app/` |
| LoftSpace applicant app — **FE Increment B: My Applications status tracker** | ✅ Done (32ffcca) — new `GET /api/applications?applicant=` reads the `leaseApplicationComplete` lens rows from the shared `weaver-targets` read model (P5: a projection, never Core KV) and scopes them to one applicant; unit-tested. The **My Applications** tab renders one card per application: the leased-unit header (`unitAddress`/`unitRent`), an "In review / complete" decision banner, and a journey-order **stepper** (onboarding → background check → payment → sign lease) whose per-step state (Done / In progress / To do) derives from the gap + `inflight_` columns. Applying routes here with the new application highlighted. **Caught + fixed live:** `maxretries_<g>` is the lens's *constant integer* retry-budget cap (not a flag) — a `bool` typing dropped every row on decode (now `int`, with a regression-guard test). **In-browser verified** against the live stack (seeded an applicant + application; bgcheck/payment auto-completed, onboarding/sign open → stepper rendered exactly that, no console errors). All gates green; lead-reviewed. Increments C (tasks) → D (documents) → E (up-full wiring) next. | `cmd/loftspace-app/applications.go`, `cmd/loftspace-app/web/` |
| **BUG → ✅ RESOLVED — TRUE root cause was a reader field-name bug (3rd investigation)** | ✅ Done (Steward, 2026-06-25). The LoftSpace task inbox showed empty (`/api/tasks` = 0 despite open assigned tasks). **TRUE root cause: `cmd/loftspace-app/tasks.go` decoded the actor field as `actorKey`, but the `my-tasks` actor-aggregate envelope stamps it as `assignee` (the lens ActorField) — so the reader skipped EVERY row.** Proven by `nats kv get my-tasks` showing a *perfect* projected row (both tasks, self-describing) whose actor field is `assignee`, not `actorKey`. Fix: decode/scope on `assignee`; corrected the unit-test fixtures (they had *fabricated* the `actorKey` shape → passed against the bug → now match the real envelope). **Verified live + in-browser:** the Tasks tab renders both userTasks, completion submits green through the real Processor. NOT a Refractor bug, NOT environmental (reproduces 100% on a clean stack). The 2nd run's engine-soundness findings + the ★★ operationName-null fix (`op.canonicalName.data.value` → `op.data.operationType`; package ops carry no canonicalName aspect) **stand and are correct** — only its "environmental" conclusion about the app symptom was wrong (it validated the lens output, never the app endpoint). All gates green; lead-reviewed. | `implementation-artifacts/mytasks-assignedto-trigger-bug.md`, `cmd/loftspace-app/tasks.go`, `packages/orchestration-base/lenses.go` |
| LoftSpace applicant app — **FE Increment C: task inbox** | ✅ Done (ced082d) — new `GET /api/tasks?applicant=` flattens the **`my-tasks` lens** rows (orchestration-base read model — P5: a projection, never Core KV; Loupe scans Core KV only as the inspector) and scopes them to one applicant; the assembler is unit-tested (scoping, openTasks flatten, the degenerate `{taskKey:null}` collect artifact, undecodable rows). The **Tasks** tab renders one card per open task — the lens-resolved op **name + description** (self-describing via the `forOperation` meta aspect-hop), the scoped target, and the expiry — with a **Complete** action. Completion drives the bound op through the existing `/api/op`: `RecordIdentityPII` (class `identity`, ssn+dob, targeted at the `scopedTo` identity) and `SignLease` (class `leaseapp`, targeted at the `scopedTo` leaseapp); the target comes from the task's `scopedTo` (§10.5 userTask invariant: assignee == scopedTo == subject). An op without a completion descriptor links to Loupe (a generic DDL-self-describing form needs an op-catalog *read model* — a Core-KV op-meta scan would violate P5 in a vertical app; noted as a follow-up). **Both completion op shapes verified end-to-end through the REAL Processor** (`.ssn`/`.dob` + `.signature` aspects landed); the Tasks tab renders + scopes in-browser with no console errors. All gates green incl. STRICT P5 lint (loftspace-app stays **zero** Core-KV reads); lead-reviewed (write path proven live). Increments D (documents) → E (up-full wiring) next. | `cmd/loftspace-app/tasks.go`, `cmd/loftspace-app/web/` |
| LoftSpace applicant app — **FE Increment A.1: repoint reads to lens projections (P5 fix)** | ✅ Done — the Core-KV scans are gone. (1) New **`availableListings` lens** (loftspace-domain → `loftspace-listings` NATS-KV read model); `/api/listings` reads that bucket. **Live-verified**: seeded a unit → the Refractor projected it → the app rendered it from the lens → applied through it. (2) `/api/identities` scan **removed** — the switcher is now a persisted identity-key field (the proper roster lens stays the lower-priority follow-up below). (3) Bundled a **deterministic P5 lint gate** (`scripts/lint-conventions.go`): any non-allowlisted `cmd/*` app referencing `core-kv` fails the build — proven to fire on the pre-fix code, clean on the fix. loftspace-app now has **zero** Core-KV reads. All gates green incl. `verify-package-loftspace-domain` (67). | `packages/loftspace-domain/lenses.go`, `cmd/loftspace-app/`, `scripts/lint-conventions.go` |
| LoftSpace applicant app — **FE Increment D: documents** | ✅ Done — new `objects-base` **`objectAttachments`** display lens (actorAggregate, anchor=object) projects per-object `storeName`/`contentType`/`size` + collected owner keys into `weaver-targets` — the **P5-clean byte-plane read model** a vertical app reads instead of Core KV (a genuine platform gap: `objectLiveness` is GC-shaped + projects no display metadata). `cmd/loftspace-app/objects.go` lifts Loupe's handler **P5-cleanly**: upload streams to the Object Store + `AttachObject` with `reads=[targetKey]` (no Core-KV probe); view/list resolve from the lens (never Core KV); the CSP/`octet-stream` guard is kept. The **Documents** tab (scope selector = identity + applications · slot picker · upload · doc cards w/ View · session-detach) **in-browser verified** against `make up-full`+`install-loftspace` (upload→list→view→detach end-to-end through the real Processor, zero console errors). cypher (3) + assembler (2) tests; `make verify-package-objects-base` 32-OK; STRICT-P5 clean (loftspace-app **zero** Core-KV refs). Cross-session detach deferred (lens can't project `type(r)`/linkName). |
| LoftSpace applicant app — **FE Increment E: local-stack wiring** | ✅ Done — new **`make up-loftspace`** one-command target: `up-full` → `install-loftspace` → builds + starts `loftspace-app` in the background on :7788 alongside Loupe (:7777), so Andrew sees the demand-side applicant view next to the operator/inspector. `down` now also reaps `loftspace-app`. Completes the LoftSpace applicant FE (A–E all shipped). | `Makefile` | `implementation-artifacts/loftspace-documents-increment-d.md`, `packages/objects-base/lenses.go`, `cmd/loftspace-app/objects.go` |
| **objects-base v1b GC — concurrent-attach reclaim race** | ✅ Done (Steward, 2026-06-25) — the attach-adjacency-lag data-loss race is closed. **Fix (design §21):** the `objectLiveness` lens no longer decides orphan-ness from the lagging adjacency `count(owner.key)=0`; it decides on a new **atomic `data.liveLinks` counter** maintained by every Attach/Detach in the SAME batch as the link mutation (`write_vertex`) — lag-free, so a freshly-attached object (link + `liveLinks=1` committed atomically) is never mis-seen orphaned during projection catch-up. Plus an **authoritative op-layer backstop**: `TombstoneObject` refuses when hydrated `liveLinks>0` (never reaps a still-linked object, independent of the lens; also closes the force-tombstone-a-live-object → revive-undercount hazard). The §20 epoch-CAS (re-link race) is preserved. **Tradeoff (supersedes §20.1 C-b):** dead-target reaping (a dangling link to a tombstoned owner) is no longer done by this lens — adjacency could not distinguish it from a not-yet-projected fresh attach (the engine skips a dead-neighbour edge entirely) — so it becomes a **bounded leak**, owned by the deferred owner-cascade trigger (see new row below). No frozen contract touched (lens BodyColumns / `TombstoneObject` sig / Weaver dispatch unchanged). **3-layer adversarial review** (ACCEPT/SHIP/FIX-FIRST → all findings addressed: the op-backstop, a replace-leg test, §21 design doc). Gates green incl. `make test-object-gc` heavy e2e, STRICT P5/conventions lint, golangci-lint. | `packages/objects-base/{lenses.go,ddls.go}`, `_bmad-output/implementation-artifacts/large-file-binary-design.md` §21 |
| **objects-base GC — dead-target owner-cascade trigger** | ✅ **Done (Steward, 2026-06-26, `4e34adc`; Andrew ratified Option A + the 4th kernel actor).** The §21.2 dead-target byte LEAK is closed. **Built** exactly as designed: a 2nd durable consumer in `object-store-manager` over the core-kv KV stream (`$KV.core-kv.vtx.*.*` roots) reacts to the **authoritative** owner-tombstone (zero projection lag → no §21-class data-loss risk), enumerates the dead owner's live `lnk.object.>` links (new `substrate.KVListKeysPrefix` over nats.go `ListKeysFiltered`), and submits **`DetachObject`** per link (deterministic requestId → Contract #4 tracker-collapse idempotency; publish-then-ack CC10; already-detached skipped; `DeliverAll` gives a one-time first-deploy catch-up that closes pre-existing leaks then runs incrementally). The existing **Loop A+B** reclaims the now-orphaned object — **the cascade adds zero new reap path, only detaches**. Kernel change (ratified): a **4th primordial service actor** (`object-store-manager` identity + `holdsRole→operator`; operator already grants DetachObject — no permission change), Bridge-template; bootstrap v10→v11, `PrimordialVertexKeyCount` 29→31. **No frozen contract touched.** Tests: 6 cascade unit cases + the heavy `make test-object-gc` `TestObjectGC_OwnerTombstoneCascadeReclaims` (owner-tombstone → cascade → Loop A+B reclaim, end-to-end, **green**) + the substrate-helper test + bootstrap actor tests moved to 4-actors/8-entries/count-31/version-11. **3-layer-reviewed** (Blind/Edge/Acceptance — see commit). All gates green (build/vet/golangci-0/STRICT-conventions-0/full-suite-61ok/e2e). Design + grounding: `large-file-binary-design.md` **§22**. *(Was: 🏗️ design-committed / 📐 awaiting-ratification; before that 📋 Bug — fallout of the §21 fix:* now that `objectLiveness` decides on the atomic `liveLinks` counter (not adjacency), an object whose owner is **tombstoned** keeps a stale `liveLinks ≥ 1` (the owner-death never touches the object) → its bytes **leak** (never reaped). This was bounded + non-permanent and strictly better than the prior data-loss bug.)* | `packages/objects-base`, `cmd/object-store-manager`, `_bmad-output/implementation-artifacts/large-file-binary-design.md` **§22**, §18/CC4, §21.2 | a 2nd durable consumer in `object-store-manager` over the core-kv KV stream (`$KV.core-kv.vtx.*.*` roots) reacts to the **authoritative** owner-tombstone (zero projection lag → no §21-class data-loss risk), enumerates the dead owner's live `lnk.object.>` links (new thin `substrate.KVListKeysPrefix` over the verified nats.go `ListKeysFiltered`), and submits **`DetachObject`** per link (deterministic requestId → Contract #4 tracker-collapse idempotency; `UnknownLink`=already-done). The existing Loop A+B (`objectLiveness` `liveLinks=0` → Weaver `directOp(TombstoneObject)` → byte-reclaim) does the rest — **the cascade adds zero new reap path, only detaches**, mirroring the identity-hygiene-merge link-enumeration precedent (CC4). Safety, idempotency, owner-revival/multi-owner/object-as-target edges, scale follow-up (reverse index) all analyzed. **THE ANDREW GATE (§22.5):** the fix needs a graph mutation, so the op-free byte-janitor gains a **4th kernel-seeded service actor** (`object-store-manager` identity + `holdsRole→operator`; operator already grants DetachObject so NO permission change) — a verbatim Bridge-actor-template addition (additive, revertible, **no frozen contract** per §21.2), but expanding the root-equivalent graph-mutating actor set 3→4 is a trust-surface call in Andrew's GC domain. Everything else is L2 (Winston): the cascade consumer, the substrate helper, unit + heavy-e2e tests, board flip — built on ratification. **Recommendation: ratify the fold-in.** *(Was: 📋 Bug — fallout of the §21 fix:* now that `objectLiveness` decides on the atomic `liveLinks` counter (not adjacency), an object whose owner is **tombstoned** keeps a stale `liveLinks ≥ 1` (the owner-death never touches the object) → its bytes **leak** (never reaped). This is bounded + non-permanent and strictly better than the prior data-loss bug, but it needs the **owner-tombstone-cascade trigger** (CC4 trigger-side, "Andrew's GC domain" per §19): on a vertex tombstone, drive a reclaim / `liveLinks` recompute for each object with an inbound link to the dead vertex (event-driven, not a sweep; §18 CC4 sketches a `KV_core-kv` `$KV.core-kv.vtx.>` durable consumer or a self-re-arming per-object heartbeat). Until then, dead-owner objects leak.)* | `packages/objects-base`, `cmd/object-store-manager`, `_bmad-output/implementation-artifacts/large-file-binary-design.md` **§22** (design), §18/CC4, §21.2 |
| Applicant switcher — **identity roster lens** | ✅ Done — new loftspace-domain **`applicantRoster`** lens projects one row per NAMED identity (`i.name.data.value`, `<> null` filter excludes unnamed service actors) into the `loftspace-identities` read model; `GET /api/identities` reads it (P5-clean). The top-right picker is now a name **dropdown** (not a typed `vtx.identity.<id>`), and the Documents scope reads "Your identity (Name)". **In-browser verified** (Alice Renter / Bob Tenant picker, scoped views, zero console errors). Noise is bounded by the name-presence filter; per-user scoping stays a Personal-Lens/read-path-auth concern (deferred) — fine under the trusted-tool model. *(Note: a fresh `make down && make up-loftspace` is needed to load the new lens — same-version reinstall is skipped, the F-004 upgrade gap.)* | `packages/loftspace-domain/lenses.go`, `cmd/loftspace-app/identities.go` |
| lease-signing `leaseApplicationComplete` lens — **multi-bgcheck row collision** | ✅ Done (Steward, 2026-06-25) — the row-collision is closed. **Engine capability added:** `max`/`min` aggregators in the full cypher engine (`executor.go` `containsAggregator` / `finalizeAggregator` / new `reduceExtreme` fold + `evalFunctionCall` row-local branch); the reducer drops nulls, folds empty/all-null → null, and orders via the engine's own `compareAny` (numeric when both numeric, else lexicographic — so canonical-UTC RFC3339 reduces chronologically). **Lens fix:** `freshUntil` is now `max(CASE WHEN fresh-completed-bgcheck THEN validUntil END)` folded into the SAME aggregating `WITH` that drives the counts (the row-expanding second `OPTIONAL MATCH (applicantNode)<-[:providedTo]-(bg)` + its `id AS applicantNode` carrier are gone), so N completed-fresh bgchecks on one identity collapse to ONE row (guardOutputKeyCollision satisfied) and `freshUntil` is the LATEST validUntil (the @at re-open timer waits for the last-expiring bgcheck). Single-bgcheck case byte-identical to before → no regression. **3-layer adversarial review** (Acceptance Auditor ACCEPT + Blind Hunter + Edge Case Hunter "ship it") — two surfaced footguns hardened (multi-arg `max(a,b)` → loud arity error not silent first-arg; `max(a)+max(b)` BinaryOp → loud error not silent empty list). Gates green incl. `make test-lease-convergence` heavy e2e (the freshUntil eager-reopen leg, 150s) + STRICT P5/conventions + golangci. Tests: engine `TestReduceExtreme`/`TestExec_WithMaxMinAggregator`/`MaxNoMatchIsNull`/`MaxMinArityError`, lens `TestLeaseApplicationComplete_MultipleFreshBgchecks` (the precise regression). | `packages/lease-signing/lenses.go`, `internal/refractor/ruleengine/full/executor.go` |
| **BUG → ✅ FIXED — duplicate userTasks (Weaver re-dispatches human tasks every 30m)** | ✅ Done (Steward, 2026-06-25, found by Andrew reviewing LoftSpace — the Tasks tab showed `RecordIdentityPII`/`SignLease` duplicated 3× each). **Root cause (pre-existing, general — NOT a regression):** the §10.3 reconciler mark-lease reclaim ([reconciler.go:270](internal/weaver/reconciler.go:270)) presumes a dispatched gap action dead once its 30m mark-lease expires and re-dispatches — gated by `gapSuppressed` (skip if `inflight_<g>` set OR dispatch-count ≥ `maxretries_<g>`). The externalTask gaps (bgcheck/payment) are protected by their `inflight_<g>` companion; the two HUMAN userTask gaps (`missing_onboarding`/`missing_signature`) had **neither companion**, so every 30m the sweep re-created their task → a duplicate per pass for any application a human hasn't completed within 30m (the decline-mode demo app, never converging, made it visible fast). **Fix:** give the userTask gaps a `maxretries_<g> = 1` constant cap (create-once), exactly mirroring the existing `maxretries_bgcheck/payment` — `gapSuppressed` then stops re-dispatch after the single `CreateTask`. **No Weaver-core change, no frozen contract, no P5/P2 surface** (a package-owned lens column). **Live-verified deterministically** on a fresh stack: a normal application dispatched exactly 1 `RecordIdentityPII` + 1 `SignLease`; the weaver-targets row carries `maxretries_onboarding=1`/`maxretries_signature=1`; both `…__count` dispatch-counts reached 1 (== cap) with the gaps still open → `gapSuppressed`=true → the 30m reclaim now skips. Lens unit-tested (`TestLeaseApplicationComplete_UserTaskDispatchCaps`); all gates green. **Tradeoff (documented):** a userTask genuinely lost/expired-unfilled won't auto-recreate — the deferred FR28 role-queue + escalation concern, not auto-recreation here. | `packages/lease-signing/{retry_budget,lenses}.go` |
| **userTask dispatch idempotency — the GENERAL fix (supersedes the interim cap)** | ✅ **CODE DONE (Steward, 2026-06-26); only the §10.3 contract edit is Andrew's.** Full item built + 3-layer-reviewed (CONFORMS / ship-ready) + all gates green incl. lease-convergence e2e: (1) §2.5 `kv.Read()` keystone (`internal/processor/starlark_kv.go`, commit `3a35941` on main); (2) Weaver per-open-episode `claimId` minted at the mark CAS-create + **preserved across reclaims** (`internal/weaver/state.go`), seeding stable `deriveStableTaskID`/`deriveStableInstanceID` (`actuator.go`) threaded through `{evaluator,reconciler,strategist}.go`; (3) `CreateTask` `kv.Read` no-op branch (present-and-alive → empty mutations+events; absent/deleted → create/self-heal) + `StartLoomPattern` optional stable `instanceId`; (4) interim `maxretries_onboarding/_signature` cap **reverted**. **Refinement vs design §4.4:** `triggerLoom` dedups at **Loom** (instance lives in loom-state, not Core KV — `getInstance` presence + `createInstance` CreateOnly on the stable instanceId), only `assignTask`→`CreateTask` uses the Processor `kv.Read`. The op `requestId` stays episode-scoped; only the artifact identity folds in `claimId`. Tests: claimId lifecycle, two-reclaim stability for BOTH taskId (assignTask) + instanceId (triggerLoom), CreateTask present→no-op / deleted→self-heal, StartLoomPattern instanceId. **ANDREW:** the **§10.3 amendment is applied UNCOMMITTED in the MAIN checkout** (`docs/contracts/10-orchestration-surfaces.md`) — ratify + commit it. **Deferred follow-up (nice-to-have):** a heavy e2e leaving userTasks open across a real mark-lease reclaim asserting exactly one task persists (mechanism already covered by the unit/script layer + existing CreateOnly tests). — ✅ **Design build-ready** The interim cap above is per-package + create-once-forever; the general fix makes Weaver's userTask dispatch idempotent the way Loom already is (§10.6). **Foundation:** revive the dormant per-episode `claimId` (CAS-create-minted, reclaim-preserved) to seed a deterministic `taskId` / Loom `instanceId`. **Enforcement is CONSUMER-side (Processor/Starlark), NOT a producer-side GET** — Andrew's call: a Weaver GET races the publish→commit propagation lag. **Mechanism: implement §2.5 lazy `kv.Read()` (contract/code gap)** — §2.5 already specifies Starlark can do on-demand `kv.Read()` (contextHint.reads is an optimisation, not a gate), but `starlark_runner.go` has no `kv` module today. With it, `CreateTask`/`StartLoomPattern` read the task key: present → script returns empty mutations + empty events (coherent no-op — neither fires); absent/tombstoned → create + event as normal. This beats `createIfAbsent` because events are decided coherently by the script (with commit-time mutation skipping, phantom `taskCreated` events fire even when the task is not created). No new mutation type. **Frozen-contract touches:** §10.3 only (dedup-by-claimId); §2.5 is already spec'd. Op name stays `CreateTask`. Reverts the interim cap. 3-layer + party review on build. Contract change - leave uncommitted in main for Andrew | `_bmad-output/implementation-artifacts/usertask-dispatch-idempotency-design.md` |
| LoftSpace applicant app — **declined application disposition** | ✅ Done (Steward, 2026-06-25, 3cff020) — a FAILED service outcome was invisible: it keeps the gap `missing_<g>` open exactly like a never-run check, so a declined application read as "in review forever." The `leaseApplicationComplete` lens now projects **`declined_bgcheck` / `declined_payment` / `declined`** — a standing failure no completed-fresh check supersedes (`(failed > 0) AND (complete = 0)`), aggregated on the SAME single no-WHERE `providedTo` fan as the existing counts (one-row-per-anchor guard preserved), NOT part of `violating` (declined ⊂ violating) and NOT read by Weaver dispatch (zero orchestration behavior change). The **My Applications** FE renders a red "Application declined" banner + a Declined step state (an in-flight retry still takes precedence — the check is being re-run). Lens correctness pinned by the real-cypher-engine unit tests (FailedBgcheck/FailedPayment assert `declined`, + a new `DeclinedSupersededByFreshRetry` guarding the `(complete = 0)` AND-term); FE view **in-browser-verified** (declined/in-review/complete cards render correctly, no console errors). Package-owned BodyColumns + FE only — no frozen contract, no Weaver change. **Note:** producing a declined state on the LIVE stack still needs the decline-trigger follow-up — Weaver auto-converges the bgcheck to `completed` (the fake clears any non-decline subject), so a real decline requires threading an applicant-controlled signal through Loom dispatch-reads → bridge params → the fake (the re-scoped residual below). | `packages/lease-signing/lenses.go`, `cmd/loftspace-app/{applications.go,web/}` |
| LoftSpace — **drive a decline LIVE (demo affordance)** | ✅ Done (Steward, 2026-06-25) — the declined experience is now demonstrable end-to-end on the running stack. The fakes gained a blanket **decline-mode toggle** (`FakeBackgroundCheck`/`FakeStripe` `SetDeclineAll`), wired to a bridge `BRIDGE_FAKE_DECLINE` env (comma-separated adapter names or `all`); the per-subject `*DeclineSubject` triggers stay for tests. Run `BRIDGE_FAKE_DECLINE=backgroundCheck,stripe` on the bridge (make inherits it: `BRIDGE_FAKE_DECLINE=… make up-loftspace`) and every check declines → Weaver dispatches → bridge declines → retries exhaust → the lens projects `declined` → the app shows it. **Live-verified end-to-end** (created an applicant + unit + application through `/api/op`; at t=10s the app row read `declined=true` / `declined_bgcheck=true` / `declined_payment=true`; the My Applications FE rendered the red banner + Declined Background-check & Payment steps, zero console errors). Internal reference-fakes only (defaults off, real adapters untouched) — no frozen contract, no P2/P5 surface; unit-tested (declineAll on both fakes + FailUntil-takes-precedence). **Residual (still ★★, the harder seam):** a *per-applicant selective* decline (e.g. an applicant named "Declined Tester") needs the applicant's `.name` aspect hydrated into the externalTask instanceOp — Loom currently dispatches `reads=[subjectKey]` only (`internal/loom/engine.go:1009`, contract-guarded), and the name is a separate `.name` aspect (root is `{}`, D5). That's a Loom dispatch-reads / §10.5-adjacent change — its own fire. | `internal/bridge/{fake_background_check,fake_stripe}.go`, `cmd/bridge/main.go` |
| LoftSpace property domain — **Increment 2: lease-signing integration** | ✅ Done (047a67e) — `CreateLeaseApplication` now **requires** a live location-domain `unit` + writes the `appliesToUnit` link + optional `.terms` aspect (moveInDate/leaseTermMonths/requestedRent); the `leaseApplicationComplete` convergence lens walks `appliesToUnit` and projects `unitKey`/`unitAddress`/`unitRent` as **informational** columns (NOT in `violating` — unit is required at create, no `missing_unit` gap). One-row-per-anchor preserved (0..1). Cross-package e2e (`make test-lease-convergence`) mints the unit via `CreateLocation`+`SetListing`/`SetUnitAddress` and asserts the columns project end-to-end; 2 new cypher tests + UnknownUnit guard. All gates green; lead-reviewed. | `packages/lease-signing/{scripts,ddls,lenses}.go` |
| Loupe agent-activity console *(★ experience — deferred)* — **design** | ⏸️ Shelved by Andrew (2026-06-25) — read-seam options rejected; revisit once the dependency map + ops-state data-home mature. Design retained; **do not build now.** Experience layer redirected to the vertical-app FEs. | `implementation-artifacts/loupe-agent-activity-console-design.md` |
| Clinic app *(★★★ experience — 2nd vertical FE)* — **Increment A: book · my appointments · provider schedule** | ✅ Done (Steward, 2026-06-25) — the clinic vertical is no longer headless. New **`cmd/clinic-app`** (binds `127.0.0.1:7799`, trusted admin actor like Loupe/loftspace-app): three tabs — **Book** (provider picker + datetime/duration/reason → `CreateAppointment`, with an inline "Add a provider" affordance + a "New patient" modal so a fresh stack is self-contained, and an honest "availability isn't enforced yet" note), **My Appointments** (patient-scoped cards: provider/specialty/time/reason + status badge + Cancel via `SetAppointmentStatus`), **Schedule** (provider-scoped read-only desk view). All reads are **P5-clean lens projections** — `GET /api/providers` (`clinicProviders`), `GET /api/patients` (**new `clinicPatients` roster lens**), `GET /api/appointments?patient=&provider=` (`clinicAppointments`) — **zero** Core-KV refs (STRICT-P5 gate clean); writes via `/api/op`. **Platform gap filled in the same fire:** added the **`clinicPatients`** projection lens (`clinic-patients` bucket) — a verbatim mirror of the already-3-layer-reviewed `clinicProviders`, **name-only** (DOB/email/phone are the deferred-Vault PHI, deliberately NOT projected — verified live the roster row carries name only). **Live + in-browser verified** against a fresh `make up-clinic`: drove CreateProvider→CreatePatient→CreateAppointment→SetAppointmentStatus(cancelled) through the REAL Processor; all three lenses projected (~1s); the FE rendered the patient switcher, the joined My-Appointments card, the provider Schedule, and the bad-status enum reject — **zero console errors**. New `make up-clinic` (up-full → install-clinic → clinic-app on :7799 alongside Loupe :7777) + `make run-clinic-app`; `make down` reaps it. M-sized (FE + 1 mirror lens, no capability/contract surface) → lead-reviewed; all gates green (build/vet/golangci/STRICT-P5/gofmt + clinic-domain & clinic-app unit + `verify-package-clinic-domain` 158-OK). UX: `implementation-artifacts/clinic-app-ux.md`. **Next:** Increment 2 = scheduling/availability + `@every` (the platform gaps), reschedule, calendar grid. | `cmd/clinic-app/`, `packages/clinic-domain/lenses.go`, `implementation-artifacts/clinic-app-ux.md` |
| `clinic-domain` package *(★★★ experience — 2nd vertical foundation)* — **Increment 1: bookable domain** | ✅ Done (Steward, 2026-06-25) — the clinic vertical's foundation. New **self-contained** `packages/clinic-domain` owning three vertex types (`patient`/`provider`/`appointment`) with their aspects (`.demographics`/`.profile`/`.schedule`/`.status`) + links (`forPatient`/`withProvider`, appointment = later-arriving source), seven ops (`CreatePatient`/`TombstonePatient`/`CreateProvider`/`TombstoneProvider`/`CreateAppointment`/`SetAppointmentStatus`/`TombstoneAppointment`), and **two P5 projection lenses** (`clinicAppointments` joins appointment→patient→provider; `clinicProviders` rosters the booking picker). Mirrors location-domain (mint+link) + loftspace-domain (aspectType step-6 gates + projection lens) idioms. **3-layer-reviewed** (Blind Hunter SHIP · Edge Case Hunter SHIP · Acceptance Auditor ACCEPT — flags triaged: non-cascading tombstone + op-enforced 0..1 cardinality both documented as intentional/platform-deferred; `patient!=provider` structurally impossible). Aspects **NON-sensitive** by design — PHI/DOB handling is the deferred **Vault plane** (clinic is its forcing function; step-6 forbids a sensitive aspect on a non-identity vertex anyway). Double-book/availability (§06 deferred), `@every` reminders, and a Weaver convergence lens are explicitly OUT (separate items this vertical forces). Gates all green (build/vet/golangci/STRICT-P5/gofmt + package unit + `lens_cypher_test` real-engine + Processor-driven `integration_test`); `make verify-package-clinic-domain` + `make install-clinic` added; registered in `cmd/lattice-pkg`. **Next:** Increment 2 = scheduling/availability + `@every` (the platform gaps), then the clinic FE (UX→FE). | `packages/clinic-domain/`, `_bmad-output/implementation-artifacts/clinic-domain-design.md` |
| `clinic-reminders` package *(★★★ experience — the clinic vertical's FIRST orchestration)* — **@at appointment reminders** | ✅ Done (Steward, 2026-06-26) — the clinic vertical is no longer pure-CRUD: a new `packages/clinic-reminders` (depends clinic-domain + orchestration-base) ships one-shot `@at` reminders ~24h before an appointment, the build-ready slice the `@every` row flagged. It **inverts lease-signing's freshness re-open**: `clinic-domain`'s `CreateAppointment` now precomputes `.schedule.remindAt = startsAt − 24h` (canonical UTC; also normalizes startsAt/endsAt); the `appointmentReminders` weaver-target convergence lens projects `freshUntil = remindAt` (arms Weaver's `@at` one-shot, ONLY while `remindAt > $now`) + a bool `missing_reminder`/`violating` gate `(reminderSentAt=null AND remindAt<=$now AND startsAt>$now AND status<>'cancelled')`. At the deadline the `@at` fires → `MarkExpired` re-touches the appointment → re-projection with fresh `$now` → gap OPENS → Weaver dispatches `directOp(RecordAppointmentReminder)` (the objectLiveness→TombstoneObject precedent) → `.reminder.sentAt` marker → gap closes. No Loom pattern (single op), **no frozen contract** (§10.2 freshUntil + §10.8 playbook + directOp are existing mechanisms; package-owned DDLs/BodyColumns). **3-layer adversarial review** (Edge=SHIP, Acceptance=ACCEPT/no-contract-touched, Blind=FIX-FIRST → fixes applied: startsAt/endsAt canonical-UTC normalization for sound lexical compares, `freshUntil` gated on `remindAt>$now` for a one-shot wake-up, cancel-during-dispatch race documented as a bounded inert-marker limitation owned by the deferred notification-delivery point). Tests: real-engine `lens_cypher_test` (6 predicate cases incl. pending/due/sent/cancelled/past/last-minute), Processor-driven `integration_test` (the directOp write-path via the empty-class reverse index + the vertex_alive guard + overwrite-idempotency), structural `package_test` (manifest↔def, DDLs, NoScans, the §10.2↔§10.8 column seam). `make install-clinic` now installs orchestration-base→clinic-domain→clinic-reminders so `up-clinic` drives reminders live; `make verify-package-clinic-reminders` added. All gates green. **Notification delivery (email/SMS) stays the deferred real-adapter work** — this records the reminder-due fact + (next) the FE surfaces "Reminder sent". | `packages/clinic-reminders/`, `_bmad-output/implementation-artifacts/clinic-reminders-design.md` |
| Clinic app — **reschedule (move an appointment)** | ✅ Done (Steward, 2026-06-26) — the CRUD gap is closed: you could cancel but not *move* an appointment. New clinic-domain **`RescheduleAppointment`** op (8th op) rewrites the `.schedule` aspect with new startsAt/endsAt — re-deriving `remindAt = startsAt − 24h` so the **clinic-reminders `@at` re-arms** for a not-yet-sent reminder (a `.schedule` CDC re-projects the `appointmentReminders` row's freshUntil = new remindAt; a <24h move reminds immediately via the violating path) — leaving the forPatient/withProvider links + `.status` untouched. Unconditioned upsert (no aspect-read; reads=[appt_key], mirroring SetAppointmentStatus); canonical-UTC normalization (`time.rfc3339_utc`/`rfc3339_add`); the FE round-trips the existing `reason` (the clinicAppointments lens projects it), an omitted reason clears it. FE: a **Reschedule** modal (prefilled from the current time/duration) + button on active My-Appointments cards (mirrors Cancel's client gating). **Live-verified** against `make up-clinic`: create→reschedule→the lens re-projected the new times (Aug-1 15:00 → Aug-5 18:00) with reason preserved + status scheduled, through the REAL Processor. Tests: Processor-driven `TestClinic_RescheduleAppointment` (new times + remindAt re-derive + reason preserve/clear + `+02:00`→`Z` + tombstoned-reject), package_test (DDL/permission/script-guard), all gates green incl. STRICT-P5 + `verify-package-clinic-domain` (now **161 OK**). No frozen contract, no new trust surface (same operator role/scope). **Limitation (follow-up below):** an *already-sent* reminder won't re-fire on reschedule (the reminders gate keys on `reminderSentAt = null`). | `packages/clinic-domain/{ddls,permissions,package}.go`, `cmd/clinic-app/web/`, `scripts/verify-package-clinic-domain.go` |
| **clinic-reminders — re-arm an already-sent reminder on reschedule** | ✅ Done (Steward, 2026-06-26) — moving an appointment whose ~24h reminder *already fired* now re-reminds for the new time (was: silently never, because the gate keyed on `reminderSentAt = null`). **Clean mechanism (no in-script aspect read, no dispatch-reads change):** `RecordAppointmentReminder` now records `.reminder.remindedFor` (the startsAt it reminded for), routed via the playbook `Params{remindedFor: row.startsAt}` (startsAt is already an `appointmentReminders` BodyColumn — the §10.2↔§10.8 seam test validates it). The convergence gate is now the **single term `remindedFor <> startsAt`** (replacing `reminderSentAt = null`): it subsumes never-reminded (no `.reminder` → `remindedFor` null → `null <> startsAt` is *true* in the full engine → due), reminded-for-current (`remindedFor = startsAt` → false → converged), and reminded-for-stale/rescheduled (old `<>` new → true → re-open + re-arm the `@at` for the new remindAt, or dispatch now if <24h). Verified empirically (`null <> field` is truthy) + pinned by new real-engine cases (`TestReminders_RescheduledAfterSent` re-arms; `…IntoWindow` due-now; `…_Sent` converged) + Processor-driven integration (op writes remindedFor). `sentAt` stays informational. No frozen contract, package-local. **Bonus — found + fixed a pre-existing `verify-package-clinic-reminders` SCRIPT bug:** it detected the weaverTarget by a `.canonicalName` aspect, but `build.go` writes weaverTargets with only a `.spec` aspect (identity = `targetId`), so that assertion could never pass — it slipped through because **CI never ran this target**. Fixed the detection (read `.spec.targetId`) AND **wired `verify-package-clinic-domain` + `-clinic-reminders` into CI** so the clinic verify gates are now enforced (both green: 161 OK / 47 OK). | `packages/clinic-reminders/{lenses,ddls,targets,package}.go`, `scripts/verify-package-clinic-reminders.go`, `.github/workflows/ci.yml` |
| Clinic app — **provider day/week calendar grid** | ✅ Done (Steward, 2026-06-26, daecf5a) — the Schedule desk view is now a positioned calendar instead of a flat card list (the build-ready, no-platform-gate pure-FE pick the Clinic-FE row flagged). A time axis + one column per day (7 in **Week**, 1 in **Day**), each appointment a block sized to its duration and status-coloured; overlapping (double-booked) appointments lay out in **side-by-side lanes** (greedy interval-partition); the hour window auto-fits the visible appointments, never narrower than 8 AM–6 PM. **Week/Day** segmented toggle + **‹ / Today / ›** period nav + range label + "N this {period} · M total" summary; a **read-only detail panel** on block click (patient · time · status badge · reason · 🔔 reminder-sent). Reads the existing `/api/appointments?provider=` lens projection (P5-clean — no backend/contract/platform change) and filters to the visible period client-side. **In-browser verified** against the live `up-clinic` stack: seeded a provider + 2 patients + 4 appointments incl. an overlapping pair → Week grid rendered the lanes + today-highlight + reminder-sent in the detail panel, Day view summarized "2 this day · 4 total", an empty future week rendered the clean grid (no dashed box), **zero console errors**. All gates green (build/vet/golangci/STRICT-P5/gofmt + clinic-app unit). **Remaining (later):** available-**slot** picking + clinic-admin slot/availability management (depend on the scheduling/`@every` platform items). | `cmd/clinic-app/web/{index.html,app.js,style.css}` |
| Clinic app — **appointment conflict detection (double-book rejection)** — Increment 1 | ✅ Done (Steward, 2026-06-26) — the ★★★ clinic platform gap (Capability-KV §06 temporal availability) is half-closed: `CreateAppointment` now **rejects a double-book** (`SlotConflict`) at op time, enforced via the op's own Starlark logic (§06 explicitly sanctions this — **no frozen-contract touch**). Enabled by today's §2.5 `kv.Read()` builtin, so **no new platform scan/adjacency seam** was needed (the original board framing's "platform read seam" is sidestepped — a Winston decision). **Mechanism:** a new provider `.bookings` adjacency aspect (class `providerBookings`, initialized empty by `CreateProvider`) is a **declared, OCC-snapshotted** `contextHint.reads` key — the concurrency serialization point (two simultaneous bookings for one provider fail closed: the 2nd commit `RevisionConflict`s, never a silent double-book). `CreateAppointment` reads it, `kv.Read`s each candidate's LIVE vertex + status + schedule (a tombstoned vertex or a terminal cancelled/completed/noShow appointment is pruned and never blocks; gating on the **vertex** `isDeleted`, since `TombstoneAppointment` leaves the aspects), rejects on a half-open interval overlap with a still scheduled/confirmed appointment, and otherwise appends (pruning bounds the index + the kv.Read fan-out). Also added an `endsAt > startsAt` guard. The clinic-app Book tab declares the `.bookings` read + surfaces a friendly SlotConflict toast. **Processor-driven integration test** (`TestClinic_DoubleBookRejected`) pins overlap-rejected / disjoint-allowed / back-to-back-allowed / different-provider-allowed / cancelled-frees-slot / tombstoned-ignored / `endsAt<=startsAt`-rejected / index-pruning, all through the REAL Processor. Gates green (build/vet/golangci/STRICT-P5/gofmt + clinic-domain/clinic-reminders/clinic-app unit; `verify-package-clinic-domain` updated). M, package-local, lead-reviewed. **Increment 2 (no contract gate either):** reschedule-into-conflict + provider availability windows (provider `.hours` aspect). | `packages/clinic-domain/{ddls,manifest,*_test}.go`, `cmd/clinic-app/web/`, `implementation-artifacts/clinic-appointment-conflict-design.md` |
| Clinic app — **appointment conflict detection** — Increment 2a: reschedule-into-conflict | ✅ Done (Steward, 2026-06-26, 8bb7cd9) — closes the double-book *bypass* Increment 1 left open: `CreateAppointment` rejected an overlap but `RescheduleAppointment` was an unconditioned upsert, so a *move* could silently land in an occupied slot. `RescheduleAppointment` now conflict-checks the new interval against the provider's `.bookings` index exactly as `CreateAppointment` does — it **requires the provider** (validated as the appointment's *actual* provider via the deterministic `withProvider` link, `kv.Read`, so a wrong/fabricated provider can't bypass by pointing at an empty book), **skips the appointment being moved** (self-exclusion), `kv.Read`s each live candidate's schedule+status, and rejects a half-open overlap with a still-scheduled/confirmed appointment (`SlotConflict`). It **rewrites the pruned bookings index under the snapshot revision** (OCC) so concurrent bookings/moves for one provider fail closed (`RevisionConflict`). Adds the `endsAt > startsAt` guard reschedule previously lacked; `providerBookings` step-6 gate now permits `RescheduleAppointment`. The FE reschedule modal passes `provider` + declares `provider.bookings` + shows a friendly conflict toast. No frozen contract, no new trust surface (Capability-KV §06 — the op's own Starlark logic). **Live-verified through the deployed Processor:** move-into-occupied → `SlotConflict`, move-to-free → accepted, same-slot re-time → accepted. Tests: `TestClinic_RescheduleIntoConflictRejected` (overlap / self-exclusion / wrong-provider / `endsAt<=startsAt` / cancelled-frees-slot / back-to-back) + the existing reschedule test updated. Gates green incl. STRICT-P5 + `verify-package-clinic-domain` **180 OK**. M, package-local, lead-reviewed (mirrors Increment 1's 3-layer-reviewed mechanism). **Remaining Increment 2b — DESIGN fire, not a clean build (Steward note, 2026-06-26):** provider availability windows (a provider `.hours` aspect) need a **clock-free day-of-week + time-of-day membership test** on an appointment instant, but the Processor Starlark `time` module today exposes ONLY `rfc3339_utc` / `rfc3339_add` (verified, `internal/processor/starlark_builtins.go`) — **no weekday / hour-of-day extractor**. So 2b needs either a new pure `time.weekday(s)` / `time.hms(s)` Starlark builtin (a core-Processor change — its own design→review→build fire; adding an op-script time-introspection surface is a Winston implementation call, NOT an Andrew gate) or an alternative model (FE precomputes the weekday — but then the op can't independently validate). Pick the builtin path next. | `packages/clinic-domain/{ddls,*_test}.go`, `cmd/clinic-app/web/app.js`, `scripts/verify-package-clinic-domain.go` |
| Clinic app — **appointment conflict detection** — Increment 2b: provider availability windows | ✅ Done (Steward, 2026-06-26, 17edc13) — closes the §06 temporal-availability story: a provider's **opt-in business hours** now gate booking. Two **pure, clock-free Starlark builtins** were added to the Processor — `time.weekday(s)` (UTC 0=Sun..6=Sat) + `time.seconds_of_day(s)` (UTC 0..86399) — the time-introspection surface 2b's design fire flagged as missing (a Winston implementation call, not an Andrew gate; same sandbox-safe deterministic pattern as `rfc3339_utc`, so at-least-once replay stays byte-stable). A new **`SetProviderHours`** op (9th op, on the provider DDL) upserts a `.hours` aspect `{windows:[{day,openSec,closeSec}]}` (class `providerHours`; UTC seconds-of-day **integers** — exact, no mixed-width "HH:MM" lexical hazard); `CreateAppointment` + `RescheduleAppointment` read it **on demand** (`kv.Read`, §2.5 — config, NOT an OCC serialization point, so absence → unconstrained, no `CreateProvider` change / backward-compatible) and reject a booking outside the windows (**OutsideHours**; cross-UTC-day or no-matching-window). No frozen contract (Capability-KV §06's sanctioned "operation's own Starlark logic"). clinic-app gains a **"Manage availability (UTC)"** editor (compose windows → SetProviderHours, write-mostly — round-trip display needs a `clinicProviders` `.hours` projection, noted follow-up) + surfaces the OutsideHours rejection. Tests: builtin unit (`TestTimeWeekday`/`TestTimeSecondsOfDay` incl. offset-normalization, malformed, arity) + Processor-driven `TestClinic_ProviderHoursEnforced` (in/out-of-hours, boundary-inclusive open+close, wrong-day, reschedule-honours-hours, no-hours-unconstrained, windows=[]-clears, day-7/open≥close validation) + package/verify guards (verify-package-clinic-domain now asserts 9 ops + the providerHours DDL). M, package-local + a pure core builtin (no trust surface) → lead-reviewed; all gates green. **Completes the §06 conflict+availability story** (Increments 1 + 2a + 2b all shipped). | `internal/processor/starlark_builtins.go`, `packages/clinic-domain/{ddls,permissions,package,manifest}.go`, `cmd/clinic-app/web/`, `_bmad-output/implementation-artifacts/clinic-provider-hours-design.md` |
| **Wire the remaining verify-package gates into CI** | ✅ Done (Steward, 2026-06-26) — CI now runs **every** `verify-package-*` gate. Added `location-domain` / `loftspace-domain` / `service-location` (after the clinic ones), each confirmed green on a fresh stack first (35 / 67 / 51 OK) and proven to coexist on one kernel (ran sequentially on the same running stack). Closes the coverage gap that let the clinic-reminders verify-script bug hide: all 9 verify-package gates (rbac, identity, identity-hygiene, objects-base, clinic-domain, clinic-reminders, location-domain, loftspace-domain, service-location) are now enforced. | `.github/workflows/ci.yml` |
| Clinic app — **day-of-visit lifecycle transitions** (confirm · complete · no-show) | ✅ Done (Steward, 2026-06-26) — closes the PO-filed gap where the FE *rendered* per-status badges/colors but exposed no way to *reach* them (only Cancel + Reschedule). **Pure FE** — wires the already-live `SetAppointmentStatus` (enum `{scheduled,confirmed,completed,cancelled,noShow}`) into status-appropriate transition buttons on **both** the My Appointments cards (Confirm/Complete/No-show + the existing Reschedule/Cancel) **and** the Schedule-grid detail popover (the front-desk/provider desk view: Complete/No-show/Cancel, no Reschedule). Reachable set is status-gated (`scheduled→{confirm,complete,noShow}`, `confirmed→{complete,noShow}`, terminal states show no actions); no-show/cancel prompt, forward progress doesn't. A `.card-actions` layout fix stacks the badge over a **wrapping** button row (the prior 2-button single-row overflowed with 5 buttons). **In-browser verified** against the live `up-clinic` stack: scheduled→confirmed→completed through the REAL Processor + lens reproject (toasts + badge/block recolor), terminal card drops all buttons, desk-view Complete recolors the calendar block, **zero console errors**. No backend/contract/platform touch (existing op, lens `status` column, badge CSS). **Follow-on (★, pkg):** an optional `reason`/note on cancel/no-show + a `checkedIn` status (the PO's "check-in" — needs a DDL enum value, lens column already carries it). | `cmd/clinic-app/web/{app.js,style.css}` |
| LoftSpace applicant app — **in-app applicant onboarding** (new-applicant modal) | ✅ Done (Steward, 2026-06-26) — closes the PO-filed gap where a fresh stack had **zero in-app way to create an applicant** (the top-right control was switcher-only; minting an applicant needed a raw `CreateUnclaimedIdentity` with a 64-char sha256 `claimKeyHash`). Adds a **"＋ New applicant"** affordance + modal (name · email · phone) mirroring clinic-app's "New patient". **Pure FE** — the trusted-tool client mints a random claim secret + hashes it in-browser (`crypto.subtle`, a secure context on 127.0.0.1) and submits `CreateUnclaimedIdentity` (class `identity`, `{name,email,phone,claimKeyHash}`) via the existing generic `/api/op`; the plaintext never enters Lattice (NFR-S6) and is discarded (no claim ceremony in this demo). The new applicant becomes active + appears in the **`applicantRoster` lens** (P5-clean read model, never Core KV) once projected. **In-browser verified** against the live `up-loftspace` stack: created "Dana Tester" through the REAL Processor (green toast w/ the minted key), the roster lens projected her, a reload selected her in the switcher and **enabled Apply** — fully usable end-to-end, **zero console errors**. No backend/contract/platform touch (existing op + roster lens; `crypto.subtle` is browser-native). Makes loftspace-app demoable on a clean stack without the CLI. | `cmd/loftspace-app/web/{index.html,app.js}` |
| LoftSpace — **unit/listing lifecycle: concurrent/duplicate-application guard** (Increment 1) | ✅ Done (Steward, 2026-06-26) — closes the PO-filed bug where the SAME applicant could apply to the SAME unit twice and both commit (two live `vtx.leaseapp` per applicant+unit → a bare-shell that pins `health.weaver` red, the folded platform-robustness symptom). `CreateLeaseApplication` now **rejects a repeat live application by the same applicant for the same unit** (`DuplicateApplication`), enforced via the op's own Starlark logic (Capability-KV §06 — **no frozen-contract touch**) using the §2.5 `kv.Read()` builtin, so **no platform scan/adjacency seam** was needed. **Mechanism (mirrors clinic's 3-layer-reviewed double-book):** a new cross-package **`unitLeaseApplications`** aspect on the location-domain `vtx.unit` (`.leaseApplications = {applications:[{leaseApp,applicant}]}`, declared like loftspace-domain's `.listing`/`.address`) is the per-unit live-application index. The op reads it **on demand** (`kv.Read` — NOT a declared `contextHint.reads` key, because a never-applied unit has no index and a declared read would HydrationMiss), prunes tombstoned/absent entries (`kv.Read` liveness), rejects a same-applicant repeat, and rewrites the index **OCC-guarded** on the read revision (present) or **CreateOnly** (absent) — so two concurrent applications for one unit **fail closed** (the second RevisionConflicts), never a silent duplicate. **Cross-applicant applications to one unit stay allowed** (normal leasing: the landlord chooses) — the guard is per-applicant. clinic-precedent-clean, backward-compatible (absent index → unconstrained). The loftspace-app apply path surfaces a friendly "You already have an active application for this unit." toast. Tests: Processor-driven `TestCreateLeaseApplication_{DuplicateSameApplicantSameUnit_Rejected,DifferentApplicantsSameUnit_Allowed,SameApplicantDifferentUnits_Allowed,TombstonedPriorApplication_AllowsReapply}` (the last proves a withdrawn application is pruned + does not block re-apply). M, package-local + FE, lead-reviewed (mirrors the 3-layer-reviewed clinic double-book mechanism); all gates green (build/vet/golangci/STRICT-P5/gofmt + full lease-signing suite). **Known bound:** only tombstoned entries are pruned (a converged/declined application is lens-computed, not stored, so it stays in the index) — fine at demo scale; the index also feeds a future by-unit landlord lens. **Increment 2 (still 📋):** listing-status-on-approval transition (convergence → `directOp(SetListing status=leased)`, the `appointmentReminders`/`objectLiveness` precedent) so an approved unit drops out of Browse&Apply. | `packages/lease-signing/{scripts,ddls,manifest}.go`, `cmd/loftspace-app/web/app.js` |
| Clinic app — **`checkedIn` status + audit note on cancel/no-show** | ✅ Done (Steward, 2026-06-26) — closes the ★ pkg follow-on the day-of-visit lifecycle row filed. **Package (clinic-domain):** the appointment status enum gains **`checkedIn`** (an active, non-terminal state — the day-of-visit flow is now scheduled → confirmed → **checkedIn** → completed; checkedIn keeps blocking its slot in the double-book guard, correctly — it is NOT added to the terminal-prune list); `SetAppointmentStatus` gains an **optional `note`** recorded on the `.status` aspect `{value, note?}` (a cancel / no-show reason for records / billing, distinct from the `.schedule` visit reason; an unconditioned upsert, so a later noteless transition clears it — the note belongs to the transition it was recorded with). The `clinicAppointments` lens projects a new **`statusNote`** column (P5-clean BodyColumn) so the note isn't write-only. No new op / DDL / frozen contract — just enum + field + lens column. **FE (clinic-app):** `checkedIn` is wired into the transition state machine + labels + a purple badge/calendar color; cancel & no-show now **prompt for an optional note** (dismiss aborts) passed as `SetAppointmentStatus` `note`; the recorded note renders (📝) on the My-Appointments cards + the Schedule-grid detail popover. Tests: Processor-driven `TestClinic_StatusCheckedInAndNote` (checkedIn accepted · note recorded on no-show · noteless transition clears it) + the `package_test` status-enum guard updated; all gates green (build/vet/golangci/STRICT-P5/gofmt + clinic-domain/clinic-reminders/clinic-app unit). **In-browser verify deferred to a fresh `make up-clinic`** (the new clinic-domain version's enum + lens column need a fresh install — F-004 skips a same-version reinstall onto the shared running stack; verify-package-clinic-domain runs on CI's clean kernel). **Remaining day-of-visit work:** none filed beyond this. | `packages/clinic-domain/{ddls,lenses}.go`, `cmd/clinic-app/web/{app.js,style.css}` |
| LoftSpace — **listing-status transition on approval** (lifecycle Increment 2, part a) | ✅ **Done (Steward, 2026-06-26)** — an approved unit now drops out of Browse&Apply. New status-only **`SetListingStatus(unit,status)`** op in loftspace-domain (kv.Read-preserves the `.listing` economics, rewrites only `status`; idempotent **empty-mutations** no-op when already at target — no CDC churn; granted to `operator`) driven by a **cross-package `directOp`** from lease-signing's `leaseApplicationComplete` target: a new `missing_listingLeased` gap (`(unitKey<>null) AND applicantApproved AND (unitStatus<>null) AND (unitStatus<>'leased')`) folded into `violating` (load-bearing — Weaver only dispatches while violating=true) → `directOp(SetListingStatus status=leased)`. The `unitStatus<>null` guard (added at build) closes a thrash hazard the design's formula missed (a listing-less unit would dispatch → NoListing-reject forever). New `applicantApproved` column decouples the applicant FE "approved" banner from the violating term. The unit's `.listing` flip reprojects the leaseapp (appliesToUnit 1-hop neighbor — same adjacency path as `.ssn`→missing_onboarding; verified in source). Multi-applicant race + dead-unit + freshness-reopen all self-resolve. **3-layer reviewed** (Blind Hunter SHIP · Edge Case Hunter SHIP · Acceptance Auditor ACCEPT). No frozen contract, no Andrew gate. Tests: Processor-driven `TestLoftspace_SetListingStatus` (transition · preserve-economics · NoListing · dead-unit · idempotent zero-write no-op · bad-status); 6 lens cypher cases (opens/closes/4-corner-not-approved-gate/pending/no-listing); the **heavy `test-lease-convergence` e2e** (`TestLeaseConvergence_ListingLeasedOnApproval` + every existing convergence test now requires the flip). Both packages bumped 0.1.0→0.2.0; verify-package-loftspace-domain updated (CI clean-kernel gate). All gates green. | `implementation-artifacts/loftspace-listing-status-on-approval-design.md` |
| **Weaver platform-robustness — malformed anchor row degrades, not errors** | ✅ Done (Steward, 2026-06-26, 1bcdce4) — closes the folded platform-robustness seam the PO live-observed (a malformed/bare `vtx.leaseapp` anchor pinned `health.weaver` **red-forever** with a repeating `TemplateDataError`: `param "subject" references row.applicant null`). **Root cause:** the two per-row DATA-error paths (`internal/weaver/evaluator.go` — a violating row missing its `entityKey` echo, and a gap playbook template reference resolving null) surfaced a Contract #5 §5.2 **`error`** (unhealthy) Health issue, re-set every reconcile sweep, so one bad row marked the WHOLE Weaver component red while every other row remediated fine. **Fix:** downgrade both to **`warning`** (degraded) + Warn-level log, matching (a) the established RowDataError model (`boolColumn`/`intColumn`/`temporal` already surface row-data errors at warning) and (b) the **frozen Contract #5 §5.2 definition** — `unhealthy` means the component *cannot fulfil its primary responsibility*; a single un-dispatchable row does not, so it was contract-inconsistent at `error`. **Config errors stay `error`** (`GapWithoutPlaybook`, `PlaybookConfigError` — they affect every row of the target, only a package re-author can fix them) — the principled data-vs-config severity split. Dispatch behaviour (skip/Ack) unchanged; **no frozen contract touched** (the contract already mandated this — code brought into conformance). Test: `TestHandleRow_MalformedAnchorDegradesNotErrors` (both paths Ack+skip+`warning`, no op). Small well-scoped green orchestration-plane change → lead-reviewed; all gates green (build/vet/golangci/STRICT-P5). | `internal/weaver/evaluator.go`, `internal/weaver/evaluator_internal_test.go` |
| LoftSpace landlord surface — **decision op + FE (design)** | ✅ **Winston-ratified — build-ready** (Steward, 2026-06-26). `implementation-artifacts/loftspace-landlord-surface-design.md`. Resolves the crux a naive "decline" would hit — the just-shipped auto-flip leases the unit on `applicantApproved`, so a human decision must **gate** that flip, not race it. **Resolution (ratified):** split applicant *readiness* (automatic, unchanged) from the *leasing decision* (human, new) — `missing_listingLeased` re-gates from `applicantApproved` → an explicit **`landlordApproved`**, so a qualified application sits *awaiting landlord decision* until a new `DecideLeaseApplication{leaseAppKey, decision∈{approved,declined}}` op (lease-signing, operator-grant, writes a `.decision` aspect, Capability-KV §06 — no frozen contract) approves (→ flip+lease) or declines (→ terminal `declined`). Lens gains `landlordDecision`/`landlordApproved`/`landlordDeclined` columns; `declined` folds in landlord-decline; the applicant "complete" banner moves to landlordApproved+leased. **Build fire (NOT a small green follow-up):** revises the heavily-reviewed convergence semantics → wants **3-layer review** + the `test-lease-convergence` heavy e2e updated (insert a decide-step; new no-lease-until-approved + decline-terminal cases) + lease-signing 0.3.0→0.4.0. FE = a UX→FE fire (My units + Post-a-listing form + applicants-per-unit Approve/Decline over `/api/unit-applications`). No Andrew gate. | `implementation-artifacts/loftspace-landlord-surface-design.md` |
| LoftSpace landlord surface — **data layer: by-unit applications view** | ✅ Done (Steward, 2026-06-26, e6d4499) — the first foundation slice of the ★★★ landlord/property-manager surface (the #1 product gap). New P5-clean **`GET /api/unit-applications`** in loftspace-app shows every listed unit + the live applications against it, each with the applicant's **name** and convergence **disposition** (approved/declined/in_review) + signed state. Assembled **entirely from existing lens read models** (`leaseApplicationComplete` weaver-targets rows grouped by `unitKey` + `applicantRoster` names + `availableListings` units) — **no new lens, no package version bump, no contract surface**. A listed unit with zero applications still appears (whole-inventory view); a unit that lost its listing surfaces from the convergence row; unitless/malformed rows skipped. `groupByUnit` pure assembler unit-tested (grouping, name-join, status/signed derivation, zero-app + listing-less cases, deterministic sort); **live-verified** against the running stack (grouped 2 live applications under a real unit with the applicant name joined). All gates green incl. STRICT P5 (loftspace-app stays **zero** Core-KV reads). **Remaining = the landlord FE tab (UX→FE)** + the harder explicit-landlord-decision op (approve/decline vs. today's auto-converge). | `cmd/loftspace-app/unit_applications.go` |
| _all other items_ | 📋 Backlog | see themed tables below |

---


## Done / moot — *not backlog*

- **Per-lens delete mode (Story 1.5.12)** — built; `deleteMode` (default hard) is in use across the
  task / ephemeral lenses.
- **§10.8 nudge-`operation` CAR** — moot: 13.5 retired the nudge GapAction; external remediation is now
  `triggerLoom` of an `externalTask` via the bridge.
- **Capability-Lens god-cypher → contract-contribution** — resolved (Epic 12).

---

*Consolidates and supersedes: `lattice-architecture.md` "Open Items (Phase 3+)" (OI-1 async adapters /
OI-2 large files carried here), `epics/index.md` "Phase 2+ Deferred Architectural Capabilities", and the
per-component "Deferred (Phase 3+)" sections.*
