name: identity-domain
version: 0.1.0
description: |
  Identity vertex creation, claim, and state-machine management. Provides
  CreateUnclaimedIdentity, UpdateIdentityState, ClaimIdentity,
  RecordIdentityPII. Depends on rbac-domain. The consumer, frontOfHouse,
  and backOfHouse roles are declared in the package definition and created
  in the single install batch routed through the Processor (Story 1.5.5).
depends:
  - rbac-domain
declares:
  ddls:
    - canonicalName: identity
      class: meta.ddl.vertexType
    - canonicalName: ssn
      class: meta.ddl.aspectType
    - canonicalName: dob
      class: meta.ddl.aspectType
    - canonicalName: name
      class: meta.ddl.aspectType
    - canonicalName: email
      class: meta.ddl.aspectType
    - canonicalName: phone
      class: meta.ddl.aspectType
    - canonicalName: claimKey
      class: meta.ddl.aspectType
    - canonicalName: credentialBinding
      class: meta.ddl.aspectType
  permissions:
    - operationType: CreateUnclaimedIdentity
      scope: any
      grantsTo: [frontOfHouse, backOfHouse, operator]
    - operationType: UpdateIdentityState
      scope: any
      grantsTo: [operator]
    - operationType: ClaimIdentity
      scope: self
      grantsTo: [consumer]
    - operationType: RecordIdentityPII
      scope: any
      grantsTo: [frontOfHouse, backOfHouse, operator]
