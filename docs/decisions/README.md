# Architecture Decision Records

Durable, code-adjacent decision records. These capture *why* a structural choice was made (or is being weighed), so the rationale survives context switches.

Relationship to the planning ADR sequence: the canonical, ratified ADR-NN numbering lives in `_bmad-output/planning-artifacts/lattice-architecture.md` (planning-owned). Records here use a local `NNNN` sequence and, when in `Proposed` status, feed a decision into an architecture session before any canonical number is assigned.

| ADR | Status | Title |
|-----|--------|-------|
| [0001](./0001-refractor-lens-projection-generalization.md) | Proposed | Refractor per-actor lens projection: generalization, capability decomposition, and projection-plane integrity |
